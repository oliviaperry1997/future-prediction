---
phase: 07-eastern-asia-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/entity-config.json
autonomous: true
requirements:
  - EURA-02
user_setup: []
must_haves:
  truths:
    - "Eurasia and Eastern Asia KML folders no longer have (wip) tags"
    - "Unified Korea entity removed from entity-config.json; ROK and DPRK entities added"
    - "China entity config includes HKG and TWN country codes in a group entry"
    - "KML folder hierarchy uses ROK/DPRK instead of North Korea/South Korea"
    - "Mongolia entity config has section_anchor 'mongolia' and see_path referencing borders-geopolitics.md#mongolia"
    - "Japan KML descriptions no longer reference #japan anchor (bold text, not a heading)"
  artifacts:
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Updated Eastern Asia KML without (wip) tag, with DPRK/ROK folder+Placemark names, Mongolia description, fixed Japan descriptions"
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Updated entity config with ROK, DPRK entities, China HKG+TWN group codes, consistent folder names"
      contains: "ROK and DPRK entity entries; China with HKG+TWN country_codes; no Unified Korea; Eastern Asia folder without (wip)"
  key_links:
    - from: "entity-config.json::folder_hierarchy Eastern Asia"
      to: "DPRK, ROK entity names"
      pattern: "Folder names match entity names exactly: DPRK, ROK"
    - from: "entity-config.json::entities China"
      to: "HKG and TWN country codes"
      pattern: "country_codes: [\"CHN\", \"HKG\", \"TWN\"]"
    - from: "borders.kml::Mongolia Placemark"
      to: "borders-geopolitics.md#mongolia"
      pattern: "See: 2050-snapshot/domains/borders-geopolitics.md#mongolia"
---

<objective>
Fix KML representation of Eastern Asia in borders.kml and update entity-config.json: remove (wip) tags from Eurasia and Eastern Asia folders, rename Korea folders to DPRK/ROK, add Mongolia description, fix Japan description anchors, update China entity config to group entry with HKG+TWN, and add separate ROK/DPRK entity configs replacing Unified Korea. Per D-11, D-12, D-13, D-14, D-15.

Purpose: Eastern Asia's KML representation must reflect the 2050 geopolitical reality (two Koreas on diverging trajectories, not unified; Mongolia as documented buffer; China with HKG+TWN territories) and be marked as reviewed (no wip).
Output: Updated `borders.kml` and `entity-config.json` with corrected Eastern Asia representation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-eastern-asia-review/07-CONTEXT.md

<interfaces>
**Current entity-config.json Eastern Asia folder (lines ~220-225):**
```json
"Eastern Asia (wip)": [
  "China",
  "Japan",
  "Mongolia",
  "North Korea",
  "South Korea"
],
```

**Current entity-config.json China entry (lines ~963-976):**
```json
"China": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "CHN",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "china",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#china"
},
```

**Current entity-config.json Japan entry (lines ~981-989):**
```json
"Japan": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "JPN",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "japan",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#japan"
},
```

**Current entity-config.json Unified Korea entry (lines ~990-1001):**
```json
"Unified Korea": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "country_codes": ["KOR", "PRK"],
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "korea-unified",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#korea-unified"
},
```

**Current entity-config.json Mongolia entry (lines ~1492-1503):**
```json
"Mongolia": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "MNG",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md"
},
```

**Current borders.kml Eastern Asia folder (line ~46072+):**
```xml
<Folder>
  <name>Eastern Asia (wip)</name>
  <Folder><name>China</name>...</Folder>
  <Folder><name>Japan</name>...</Folder>
  <Folder><name>Mongolia</name>...</Folder>
  <Folder><name>North Korea</name>...</Folder>
  <Folder><name>South Korea</name>...</Folder>
</Folder>
```

**Current borders.kml Japan description (used on all Japan Placemarks):**
`<description>See: 2050-snapshot/domains/borders-geopolitics.md#japan</description>`

The `#japan` anchor references bold text `**Japan:**` in borders-geopolitics.md (line 379), which is NOT a markdown heading and does not generate a reliable HTML anchor. Per D-15, fix to use a stable anchor format.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove (wip) tags from Eurasia and Eastern Asia in entity-config.json and borders.kml</name>
  <files>2050-snapshot/kml/entity-config.json, 2050-snapshot/kml/borders.kml</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (lines 208-226 for Eurasia and Eastern Asia entries in folder_hierarchy)
    - 2050-snapshot/kml/borders.kml (around line 46072 for Eastern Asia folder name; also check line ~45838 for Eurasia folder name)
  </read_first>
  <action>
    Edit both files to remove all (wip) tags from Eurasia and Eastern Asia:

    **1. entity-config.json — folder_hierarchy section:**
    - Change `"Eurasia (wip)"` → `"Eurasia"` (line ~208)
    - Change `"Central Asia (wip)"` → `"Central Asia"` (line ~212) — Phase 6 already cleaned KML but entity-config may still have the tag
    - Change `"Eastern Asia (wip)"` → `"Eastern Asia"` (line ~220)

    **2. borders.kml:**
    - Change `<name>Eastern Asia (wip)</name>` → `<name>Eastern Asia</name>` (line ~46072)
    - Verify that `<name>Eurasia (wip)</name>` at line ~45838 is already clean (Phase 6 should have handled this). If the (wip) tag still exists there, remove it: `<name>Eurasia (wip)</name>` → `<name>Eurasia</name>`

    **Important:** Only change the (wip) tag removals. Do NOT change any other content.
  </action>
  <verify>
    <automated>grep -c 'Eastern Asia (wip)' 2050-snapshot/kml/borders.kml 2050-snapshot/kml/entity-config.json; echo "both should be 0"; grep -c 'Eurasia (wip)' 2050-snapshot/kml/borders.kml 2050-snapshot/kml/entity-config.json; echo "both should be 0"</automated>
  </verify>
  <done>
    Eurasia and Eastern Asia folders no longer have (wip) tags in borders.kml or entity-config.json. Per D-11.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update entity-config.json — add ROK/DPRK entities, remove Unified Korea, update China with HKG+TWN, fix Mongolia anchor</name>
  <files>2050-snapshot/kml/entity-config.json</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (China entry ~963-976, Unified Korea ~990-1001, Mongolia ~1492-1503, folder_hierarchy Eastern Asia ~220-225 after (wip) removal)
  </read_first>
  <action>
    Make the following edits to entity-config.json:

    **A. Update Eastern Asia folder_hierarchy (after (wip) removal in Task 1):**
    Change entity names to match the corrected geopolitical reality:
    ```json
    "Eastern Asia": [
      "China",
      "Japan",
      "Mongolia",
      "DPRK",
      "ROK"
    ],
    ```
    ("North Korea" → "DPRK", "South Korea" → "ROK")

    **B. Update China entity (lines ~963-976):**
    Change from single country_code to group with HKG+TWN per D-13:
    ```json
    "China": {
      "type": "entity",
      "category": "global",
      "source": "group",
      "country_codes": ["CHN", "HKG", "TWN"],
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "china",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#china"
    },
    ```
    Key changes: `"source": "group"` (was "country"), `"country_codes": ["CHN", "HKG", "TWN"]` (was `"country_code": "CHN"`). Note the field name change from `country_code` (singular) to `country_codes` (plural array) — match the pattern used by Unified Korea and other group entities.

    **C. Delete Unified Korea entity (lines ~990-1001):**
    Remove the entire `"Unified Korea": { ... },` block including the trailing comma. Per D-12.

    **D. Add ROK and DPRK entity entries:**
    Insert these two entries immediately after the China entry's closing `},` (around line ~976). They replace Unified Korea per D-01, D-02:
    ```json
    "DPRK": {
      "type": "entity",
      "category": "global",
      "source": "group",
      "country_codes": ["PRK"],
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "dprk",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#dprk"
    },
    "ROK": {
      "type": "entity",
      "category": "global",
      "source": "group",
      "country_codes": ["KOR"],
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "rok",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#rok"
    },
    ```
    DPRK takes PRK country code (ISO 3166-1 alpha-3 for North Korea). ROK takes KOR (ISO 3166-1 alpha-3 for South Korea). Both are `"source": "group"` since they may reference multi-polygon KML Placemarks.

    **E. Update Mongolia entity (lines ~1492-1503):**
    Fix empty section_anchor and generic see_path per D-14:
    ```json
    "Mongolia": {
      "type": "entity",
      "category": "global",
      "source": "country",
      "country_code": "MNG",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "mongolia",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#mongolia"
    },
    ```

    **Important:** Maintain valid JSON throughout. Verify with `python3 -m json.tool` after all edits. Ensure all trailing commas are correct (last entry in object has no comma, all others do).
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)
# Check ROK and DPRK exist
assert 'ROK' in d['entities'], 'ROK missing'
assert 'DPRK' in d['entities'], 'DPRK missing'
# Check Unified Korea removed
assert 'Unified Korea' not in d['entities'], 'Unified Korea still present'
# Check China group
assert d['entities']['China']['source'] == 'group', 'China not group'
assert set(d['entities']['China']['country_codes']) == {'CHN', 'HKG', 'TWN'}, f'China codes wrong: {d[\"entities\"][\"China\"][\"country_codes\"]}'
# Check Mongolia anchor
assert d['entities']['Mongolia']['section_anchor'] == 'mongolia', 'Mongolia anchor wrong'
# Check folder hierarchy
ea = d['folder_hierarchy']['Eurasia']['Eastern Asia']
assert 'DPRK' in ea and 'ROK' in ea, f'Eastern Asia folder wrong: {ea}'
assert 'North Korea' not in ea and 'South Korea' not in ea
print('PASS: all entity-config checks passed')
"</automated>
  </verify>
  <done>
    entity-config.json: Unified Korea removed, ROK+DPRK added, China updated to group with HKG+TWN, Mongolia section_anchor populated, folder_hierarchy uses DPRK/ROK. Per D-12, D-13, D-14.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update borders.kml — rename Korea folders and Placemarks, add Mongolia description, fix Japan descriptions</name>
  <files>2050-snapshot/kml/borders.kml</files>
  <read_first>
    - 2050-snapshot/kml/borders.kml (lines ~46072-49172 covering the Eastern Asia region — from the folder start through the end of South Korea Placemarks)
    - 2050-snapshot/kml/borders.kml (Mongolia Placemark around ~48407-48422, Japan folder start ~46987)
  </read_first>
  <action>
    Make the following edits to borders.kml. These are all within the Eastern Asia folder (after `(wip)` removal from Task 1):

    **A. Rename Korea folders and Placemarks** — use sed-style global replacement within the Eastern Asia section:
    - Change ALL occurrences of `<name>North Korea</name>` → `<name>DPRK</name>` (folder name + all Placemark names, approximately lines 48423-48544)
    - Change ALL occurrences of `<name>South Korea</name>` → `<name>ROK</name>` (folder name + all Placemark names, approximately lines 48546-49172)

    Use syntax like: `sed -i '' '/Eastern Asia/,/Folder.*name.*Eastern/ s/<name>North Korea<\/name>/<name>DPRK<\/name>/g' borders.kml`

    **B. Add Mongolia description** — find the Mongolia Placemark (around line 48407, identifiable by `<name>Mongolia</name>` inside the Mongolia folder). Currently the Placemark has no `<description>` field. Add after the `<styleUrl>` line and before `<Polygon>`:
    ```xml
    <description>See: 2050-snapshot/domains/borders-geopolitics.md#mongolia</description>
    ```
    Per D-14. The Mongolia Placemark currently looks like:
    ```xml
    <Folder>
      <name>Mongolia</name>
      <Placemark>
        <name>Mongolia</name>
        <styleUrl>#__managed_style_XXXXXXXXX</styleUrl>
        <Polygon>...</Polygon>
      </Placemark>
    ```
    Insert the description line after `<styleUrl>...</styleUrl>` and before `<Polygon>`.

    **C. Fix Japan description anchors** — The Japan Placemarks (all ~200 Placemarks in the Japan folder, lines ~46987-48406) currently have:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md#japan</description>`

    The `#japan` anchor references bold text `**Japan:**` (line 379 of borders-geopolitics.md), which is NOT a standard HTML anchor target (markdown bold text doesn't generate an id). Per D-15, remove the anchor fragment:
    Replace all Japan Placemark descriptions from:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md#japan</description>`
    to:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md</description>`

    Use sed-style replacement within the Japan folder section:
    `sed -i '' '/<Folder>/,/<\/Folder>/ s|<description>See: 2050-snapshot/domains/borders-geopolitics.md#japan</description>|<description>See: 2050-snapshot/domains/borders-geopolitics.md</description>|g' borders.kml`
    (Target only within the Japan folder by identifying its boundaries, or use a more precise range.)

    **Important:** After all edits, verify the KML is well-formed XML. Do NOT modify China Placemarks or any other entity's content.
  </action>
  <verify>
    <automated>echo "DPRK entries:" && grep -c '<name>DPRK</name>' 2050-snapshot/kml/borders.kml && echo "ROK entries:" && grep -c '<name>ROK</name>' 2050-snapshot/kml/borders.kml && echo "Old North Korea entries (should be 0):" && grep -c '<name>North Korea</name>' 2050-snapshot/kml/borders.kml && echo "Old South Korea entries (should be 0):" && grep -c '<name>South Korea</name>' 2050-snapshot/kml/borders.kml && echo "Mongolia description (should be >=1):" && grep -c 'borders-geopolitics.md#mongolia' 2050-snapshot/kml/borders.kml && echo "Old Japan anchors (should be 0):" && grep -c 'borders-geopolitics.md#japan' 2050-snapshot/kml/borders.kml</automated>
  </verify>
  <done>
    borders.kml: Korea folders+Placemarks renamed to DPRK/ROK, Mongolia description added referencing borders-geopolitics.md#mongolia, Japan descriptions fixed (no broken #japan anchor). Per D-14, D-15.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KML file edit | Local file modification — no external input, no untrusted data |
| JSON file edit | Local file modification — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | KML XML structural integrity | mitigate | Verify borders.kml is well-formed XML after edits — check no unclosed Folder/Placemark tags, no broken nesting. Use `xmllint --noout` if available. |
| T-07-02 | D | Accidental deletion of KML polygon data during sed operations | mitigate | Back up borders.kml before editing. After edits, verify all 5 entity folders (China, Japan, Mongolia, DPRK, ROK) remain present in Eastern Asia section. |
| T-07-03 | D | JSON parse error from entity-config.json edits | mitigate | Validate JSON with `python3 -m json.tool` after all edits. Check that all required entity entries exist. |
| T-07-04 | D | sed range mismatch — Korea renaming affecting China/Mongolia Placemarks with similar substrings | mitigate | Use targeted sed ranges scoped to the Korea folders specifically. Verify China and Mongolia names unchanged after edits. |
</threat_model>

<verification>
1. Verify no (wip) tags remain in Eurasia or Eastern Asia folders (borders.kml + entity-config.json)
2. Verify entity-config.json: Unified Korea removed, ROK+DPRK added, China has HKG+TWN as group, Mongolia has section_anchor
3. Verify borders.kml: North Korea → DPRK, South Korea → ROK (folder names + all Placemark names)
4. Verify borders.kml: Mongolia Placemark has description referencing borders-geopolitics.md#mongolia
5. Verify borders.kml: Japan Placemarks reference borders-geopolitics.md WITHOUT #japan anchor
6. Verify borders.kml is well-formed XML (all Folder/Placemark tags balanced)
7. Verify entity-config.json is valid JSON (python3 -m json.tool passes)
8. Verify China and Mongolia Placemark names unchanged after Korea rename operations
</verification>

<success_criteria>
- borders.kml: Eastern Asia folder has no (wip) tags, uses DPRK/ROK names, has Mongolia description with #mongolia anchor, fixed Japan descriptions (no broken #japan)
- entity-config.json: ROK and DPRK entities exist, Unified Korea removed, China is group with HKG+TWN country_codes, Mongolia has section_anchor "mongolia", folder_hierarchy uses DPRK/ROK
- KML opens correctly in Google Earth Pro (human verify in Plan 07-02 or post-execution)
</success_criteria>

<output>
After completion, create `.planning/phases/07-eastern-asia-review/07-01-SUMMARY.md`
</output>
