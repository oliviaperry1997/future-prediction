---
phase: 21-climate-kml-work
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - 2050-snapshot/kml/generate-climate-layers.py
  - 2050-snapshot/kml/climate.kml
autonomous: true
requirements:
  - CLMKML-02
user_setup: []

must_haves:
  truths:
    - "Ecological biomes layer appears as a folder in climate.kml with 6 distinct biome types (tundra, boreal forest, temperate forest, grassland/savanna, desert, tropical rainforest)"
    - "Biomes layer uses a visually distinct color scheme from the Köppen layer"
    - "Sea level rise inundation polygons appear as a folder in climate.kml with 6 named coastal region polygons"
    - "SLR polygons use semi-transparent blue fill appropriate for overlaying on entity boundaries"
    - "Both layers have `See:` cross-references back to relevant sections in climate.md"
  artifacts:
    - path: "2050-snapshot/kml/generate-climate-layers.py"
      provides: "Updated script with biomes and SLR generation functions"
      contains: "def generate_biomes_kml|def generate_slr_kml"
    - path: "2050-snapshot/kml/climate.kml"
      provides: "Updated with Ecological Biomes and Sea Level Rise folders"
      contains: "Ecological Biomes|Sea Level Rise Inundation"
  key_links:
    - from: "generate-climate-layers.py"
      to: "source/wwf_ecoregions/*.shp"
      via: "Fiona reads WWF ecoregions, reclassifies biome types"
      pattern: "fiona\\.open|ecoregions"
    - from: "generate-climate-layers.py"
      to: "source/dem_tiles/"
      via: "Rasterio reads DEM tiles, extracts ≤0.35m elevation zones"
      pattern: "rasterio\\.open|dem|inundation"
    - from: "climate.kml"
      to: "2050-snapshot/domains/climate.md"
      via: "See: descriptions in biomes and SLR placemarks"
      pattern: "See.*climate\\.md#"
---

<objective>
Generate the Ecological Biomes layer (D-10) and Sea Level Rise Inundation Polygons (D-11) as new folders in climate.kml.

**Purpose:** These two layers complement the Köppen classification. Biomes (D-10) shows actual vegetation communities distinct from climate thresholds — tundra, boreal forest, temperate forest, grassland/savanna, desert, tropical rainforest. SLR (D-11) visualizes the +0.35m sea level rise impact zones for key coastal deltas and atolls described in climate.md.

**Per D-12:** Both layers live in climate.kml as internal folders. No new KML files.
**Per D-13:** Each placemark includes `See:` cross-reference back to climate.md.
**Per D-15:** All content stays inside climate.kml.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-climate-kml-work/21-CONTEXT.md
@.planning/phases/21-climate-kml-work/21-DISCOVERY.md

**Key Decisions:**
- D-10: Ecological biomes layer — 6 types, distinct from Köppen
- D-11: SLR inundation polygons for Bangladesh, Mekong, Nile, US Gulf, Pacific atolls, Netherlands at +0.35m
- D-12: Both as folders in climate.kml
- D-13: `See:` cross-references in KML descriptions
- D-05: Data-driven where possible, narrative where not

**Biomes reclassification map (from DISCOVERY.md):**
| WWF Biome | Target Class | Hex Color |
|-----------|-------------|-----------|
| Tundra | Tundra | #A0A0A0 |
| Boreal Forests/Taiga | Boreal Forest/Taiga | #3A7D3A |
| Temperate Coniferous/Mixed/Broadleaf Forests | Temperate Forest | #5CA65C |
| Tropical/Subtropical Moist/Dry/Coniferous Forests | Tropical Rainforest | #1A5C1A |
| Temperate Grasslands/Savannas/Shrublands + Flooded Grasslands | Grassland/Savanna | #E8D44D |
| Deserts and Xeric Shrublands | Desert | #EDC58E |
| Mangroves | (Dissolve into Tropical Rainforest or omit — too narrow to render at global scale) |
| Mediterranean Forests/Woodlands/Scrub | (Dissolve into Temperate Forest) |
| Montane Grasslands/Shrublands | (Dissolve into Grassland/Savanna) |

**SLR target regions (from climate.md):**
1. Bangladesh Delta: Ganges-Brahmaputra-Meghna delta (21-25°N, 88-93°E)
2. Mekong Delta: Vietnam's rice basket (9-11°N, 105-107°E)
3. Nile Delta: Egypt's agricultural heartland (30-32°N, 30-32°E)
4. US Gulf Coast: Houston to Mobile (28-31°N, 88-96°W)
5. Pacific Atolls: Tuvalu, Kiribati, Marshall Islands, Maldives (per climate.md descriptions)
6. Netherlands: Dutch coastal zone (51-54°N, 3-8°E)

**Existing structures:**
- climate.kml currently has one `<Folder>` ("Climate") with 11 placemarks
- Plan 02 will have added the Köppen folder as first child
- This plan adds "Ecological Biomes (2050)" and "Sea Level Rise Inundation (0.35m)" as subsequent folders
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add biomes and SLR generation functions to generate-climate-layers.py</name>
  <files>2050-snapshot/kml/generate-climate-layers.py</files>
  <action>
    Add two new functions to `generate-climate-layers.py` (which already has `generate_koppen_kml()` from Plan 02):

    **Function 1: `generate_biomes_kml(output_path)`**
    
    Approach A (preferred — data-driven per D-05):
    1. Read WWF Terrestrial Ecoregions from `source/wwf_ecoregions/` using fiona. Expected file: `wwf_terrestrial_ecoregions.shp`
    2. If shapefile not found, print download instructions and fall back to Approach B (narrative-derived)
    3. Create a BIOME_NAME → TARGET_CLASS mapping (from the reclassification table above)
    4. For each target class, collect all matching ecoregion polygons, merge with shapely.unary_union
    5. Simplify each merged polygon to 0.02° Douglas-Peucker threshold
    6. For each of the 6 target classes, create a simplekml folder with styled polygon placemarks
    
    Approach B (fallback — narrative-derived from climate.md):
    - If WWF data unavailable, create approximate biome polygons from climate.md descriptions:
      - Tropical Rainforest: Equatorial band 10°N-10°S (Amazon, Congo, SE Asia) + coastal West Africa
      - Desert: Major deserts (Sahara 15-35°N Africa, Arabian 15-30°N, Gobi 40-45°N Asia, Australian interior, Kalahari 20-30°S, Atacama/Patagonia 25-50°S)
      - Grassland/Savanna: Transition zones between forest and desert (Sahel 10-18°N, East Africa savanna, Central Asia steppe, Great Plains N. America, Pampas S. America)
      - Temperate Forest: Mid-latitude zones (E. North America 30-50°N, Europe 35-55°N, E. Asia 25-50°N, SE Australia/New Zealand, S. Chile/Argentina)
      - Boreal Forest/Taiga: Northern band (50-65°N across Canada, Alaska, Scandinavia, Russia)
      - Tundra: Arctic fringe (65°N+ — Alaska, Canada, Siberia, Arctic islands)
    - Use geographic bounding polygons for each, with NOTE in description flagging as approximate
    
    8. Each biome placemark gets:
       - Name: e.g., "Tropical Rainforest"
       - Description: `Ecological biome zone — 2050 projection. See: 2050-snapshot/domains/climate.md#global-climate-state`
       - Style: PolyStyle with fill color from the biome palette (opacity 60 = 0x60 prefix), line color at full opacity

    **Function 2: `generate_slr_kml(output_path)`**
    
    This function creates SLR inundation polygons for the 6 target regions.
    
    Approach A (data-driven):
    1. For each SLR target region:
       a. Define bounding box 
       b. Open DEM tiles from `source/dem_tiles/` using rasterio
       c. Create a mask for elevation ≤ 0.35m within bounding box
       d. If the zone is a delta (Bangladesh, Mekong, Nile), also mask to river floodplain extent
       e. Polygonize the masked area using rasterio.features.shapes()
       f. Simplify polygons
    2. If DEM tiles not found, print download instructions and fall back to Approach B
    
    Approach B (narrative-derived):
    1. For each SLR region, create an approximate coastal inundation polygon using the region's known geographic extent + a buffer zone appropriate for flat deltas:
       - Bangladesh Delta: Coastal zone within 0-50km of Bay of Bengal coast (21-24°N)
       - Mekong Delta: Southern Vietnam (below 10°N, 105-107°E)
       - Nile Delta: Nile Delta fan (30-31.5°N, 30-32°E)
       - US Gulf Coast: Coastal strip from Houston to Mobile (within 20km of coast)
       - Pacific Atolls: Point placemarks or small circular buffers (~2km radius) at atoll coordinates
       - Netherlands: Dutch coastal provinces below sea level (approximate polygon from known polder boundaries)
    2. Annotate each with "APPROXIMATE — replace with DEM-derived polygon" in description
    
    3. Each SLR placemark gets:
       - Name: e.g., "Bangladesh Delta Inundation Zone"
       - Description: `Sea level rise inundation zone (+0.35m by 2050). See: 2050-snapshot/domains/climate.md#sea-level`
       - Style: Semi-transparent blue (polyColor = 6055b0b0, lineColor = ff55b0b0 — matching climate overlay convention from entity-config.json)
    
    Both functions write to separate temp files (`climate_biomes.kml`, `climate_slr.kml`) for merging.
  </action>
  <verify>
    <automated>python3 -c "
import importlib.util, sys
spec = importlib.util.spec_from_file_location('gen', '2050-snapshot/kml/generate-climate-layers.py')
mod = importlib.util.module_from_spec(spec)
sys.modules['gen'] = mod
spec.loader.exec_module(mod)
assert hasattr(mod, 'generate_biomes_kml'), 'Missing generate_biomes_kml'
assert hasattr(mod, 'generate_slr_kml'), 'Missing generate_slr_kml'
print('Both functions found')
"</automated>
  </verify>
  <done>generate-climate-layers.py has both generate_biomes_kml() and generate_slr_kml() functions with data-driven and fallback paths</done>
</task>

<task type="auto">
  <name>Task 2: Generate and merge biomes + SLR layers into climate.kml</name>
  <files>2050-snapshot/kml/climate.kml</files>
  <action>
    Execute the biomes and SLR generation, then merge into climate.kml:
    
    1. Run `python3 generate-climate-layers.py --biomes` to produce `climate_biomes.kml`
    2. Run `python3 generate-climate-layers.py --slr` to produce `climate_slr.kml`
    3. Merge both folders into climate.kml:
       - Parse existing climate.kml with lxml
       - Find root `<Document>` element
       - Add Biomes folder (after Köppen folder, before existing "Climate" folder)
       - Add SLR folder (after Biomes folder)
       - Preserve ALL existing content (Köppen layer from Plan 02 + 11 thematic placemarks in Climate folder)
       - Write back to climate.kml
    
    4. Verify:
       - climate.kml is valid XML
       - 3 new folders present: Köppen, Biomes, SLR + original "Climate" folder
       - Biomes has 6 placemarks (one per biome type)
       - SLR has 6 polygon placemarks (one per region) or point/approximate placemarks
       - Each biome has distinct color; SLR uses semi-transparent blue
       - DESCRIBLE field has `See:` cross-reference
       - Original "Climate" folder intact with all 11 thematic placemarks
    
    **Important:** This task only ADDS to climate.kml. It does NOT modify or replace existing folders. The original Climate folder with 11 thematic placemarks must remain untouched.
  </action>
  <verify>
    <automated>python3 -c "
import lxml.etree as ET
tree = ET.parse('2050-snapshot/kml/climate.kml')
ns = {'kml': 'http://www.opengis.net/kml/2.2'}
folders = tree.findall('.//kml:Folder/kml:name', ns)
fnames = [f.text for f in folders if f.text]
print(f'Folders: {fnames}')
assert 'Ecological Biomes (2050)' in fnames, 'Biomes folder missing'
assert 'Sea Level Rise Inundation (0.35m)' in fnames, 'SLR folder missing'
assert 'Climate' in fnames, 'Original Climate folder missing'
biomes_folder = None
for folder in folders:
    if folder.text == 'Ecological Biomes (2050)':
        biomes_folder = folder.getparent()
        break
biome_pms = biomes_folder.findall('.//kml:Placemark', ns)
print(f'Biome placemarks: {len(biome_pms)}')
assert len(biome_pms) >= 4, 'Too few biome zones'
print('PASS')
"</automated>
  </verify>
  <done>climate.kml contains 4 folders (Köppen, Biomes, SLR, Climate) with correct structure and cross-references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WWF ecoregions shapefile → KML biomes | Third-party ecoregion data rendered as KML polygons |
| DEM elevation data → SLR inundation | Elevation-based flood modeling (approximate, not hydrodynamic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | I | WWF biome reclassification mapping | mitigate | Document reclassification table clearly; use standard WWF biome names |
| T-21-08 | I | SLR 0.35m threshold (uniform) | accept | +0.35m is a global mean; local effects vary due to subsidence, tides, storm surge — noted in description as approximate |
| T-21-09 | D | Narrative-derived fallback polygons | accept | Marked as "APPROXIMATE" in description per D-05 discretion |
</threat_model>

<verification>
1. `python3 generate-climate-layers.py --biomes` produces climate_biomes.kml
2. `python3 generate-climate-layers.py --slr` produces climate_slr.kml
3. Merged climate.kml has 4 folders: Köppen, Biomes, SLR, Climate
4. Biomes folder has 6 biome type placemarks with distinct colors
5. SLR folder has 6 inundation polygon placemarks
6. Each placemark has `See:` cross-reference to climate.md section
7. Biomes color scheme is visually distinct from Köppen palette
8. Existing 11 thematic placemarks in Climate folder preserved
</verification>

<success_criteria>
- generate-climate-layers.py has working generate_biomes_kml() and generate_slr_kml()
- climate.kml has 4 total folders with all new layers correctly inserted
- Biomes layer has 6 biome types with distinct colors
- SLR layer has 6 zonal polygons with semi-transparent blue
- Both layers have `See:` back-references
- All existing content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/21-climate-kml-work/21-03-SUMMARY.md`
</output>
