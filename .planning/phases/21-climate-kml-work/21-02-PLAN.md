---
phase: 21-climate-kml-work
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - 2050-snapshot/kml/generate-climate-layers.py
  - 2050-snapshot/kml/climate.kml
autonomous: true
requirements:
  - CLMKML-01
user_setup: []

must_haves:
  truths:
    - "2050 Köppen-Geiger climate classification layer appears as a folder in climate.kml when opened in Google Earth Pro"
    - "All 30 Köppen sub-types are represented with distinct polygons across the global land surface"
    - "Polygons use standard Köppen color scheme for immediate legibility"
    - "Each zone has a `See:` cross-reference back to climate.md#global-climate-state"
    - "Layer has a descriptive name and is organized in a logical folder hierarchy (A= Tropical, B= Arid, C= Temperate, D= Cold, E= Polar)"
  artifacts:
    - path: "2050-snapshot/kml/generate-climate-layers.py"
      provides: "Python script containing Köppen polygonization, styling, and KML generation"
      contains: "def generate_koppen_geotiff_kml"
    - path: "2050-snapshot/kml/climate.kml"
      provides: "Existing climate KML file with new Köppen layer folder added"
      contains: "Köppen-Geiger"
  key_links:
    - from: "generate-climate-layers.py"
      to: "source/koppen_2041-2070_ssp370.tif"
      via: "Reads GloH2O GeoTIFF, polygonizes zones"
      pattern: "gdal_polygonize|rasterio.*read"
    - from: "climate.kml"
      to: "2050-snapshot/domains/climate.md#global-climate-state"
      via: "See: description in each Köppen placemark"
      pattern: "See.*climate.md#global"
---

<objective>
Generate the Köppen-Geiger climate classification layer as a new folder in climate.kml using GloH2O V3 dataset (per D-01, D-02, D-03).

**Purpose:** The Köppen layer is the primary new feature of Phase 21 — a high-detail global 2050 climate classification map with all 30 sub-types (Af through EF), derived from CMIP6 projections per D-02. This provides a geographic instantiation of the climate analysis in climate.md §Global Climate State.

**Output:** Python generation script + updated climate.kml with Köppen folder containing all 30 sub-type placemarks.

**Per D-12:** All content goes into climate.kml (no new KML files). Köppen folder sits alongside Biomes, SLR, and thematic placemarks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-climate-kml-work/21-CONTEXT.md
@.planning/phases/21-climate-kml-work/21-DISCOVERY.md

**Key Decisions to Implement:**
- D-01: 2050 Köppen map as new KML layer — all sub-types (Af-EF)
- D-02: Data-driven from research (GloH2O V3 confirmed in DISCOVERY.md)
- D-03: High detail global — file size acceptable
- D-12: Lives in climate.kml as internal folder
- D-13: `See:` cross-reference back to climate.md in each KML description

**Research finding:** Use GloH2O V3 2041-2070 SSP3-7.0 GeoTIFF (1km/0.0083° resolution). Standard Köppen color scheme documented in DISCOVERY.md.

**Existing KML structure (climate.kml):**
- Single `<Folder>` named "Climate" containing 11 placemarks with rough bounding boxes
- Uses `__managed_style_*` from Google Earth with teal (#55b0b0) overlay style
- Cross-reference format: `See: 2050-snapshot/domains/climate.md#section-anchor`
- Generated programmatically in generate-kml.py via `generate_domain_kml()` function

<interfaces>
From existing 2050-snapshot/kml/generate-kml.py patterns:
- KML namespace: xmlns="http://www.opengis.net/kml/2.2" with gx extension
- Folder structure: `<Folder><name>Layer Name</name>...</Folder>`
- Placemark format: `<Placemark><name>Zone Name</name><description>See: path.md#anchor</description><Polygon>...</Polygon></Placemark>`
- Style via `<gx:CascadingStyle>` or `<Style><LineStyle><PolyStyle>...</Style>`

Standard Köppen color map (from DISCOVERY.md):
- Af: #0000FF, Am: #0078FF, Aw: #46AAFA
- BWh: #FE0000, BWk: #FE9695, BSh: #F5A505, BSk: #FFDC7C
- Csa: #FFCC00, Csb: #C9C800, Cwa: #C6C76A, Cwb: #6C9A5E, Cwc: #7EAA7D
- Cfa: #96FF96, Cfb: #6DB46D, Cfc: #40A040
- Dsa: #5EBDC9, Dsb: #4DA6B8, Dsc: #2F8FA5
- Dwa: #5DF0F0, Dwb: #41C8C8, Dwc: #2CAAAA
- Dfa: #00FFFF, Dfb: #46D2D2, Dfc: #64C8E4, Dfd: #5EE0F0
- ET: #964696, EF: #FFFFFF
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Köppen KML generation script</name>
  <files>2050-snapshot/kml/generate-climate-layers.py</files>
  <behavior>
    - Script must be importable and have a main() entry point
    - Must handle missing source GeoTIFF gracefully with informative error
    - Must produce valid KML XML parsable by lxml
    - Must include all 30 Köppen sub-types with correct alpha-2 codes (Af, Am, Aw, etc.)
    - Each sub-type folder must be non-empty (at least one polygon) or absent
  </behavior>
  <action>
    Create `2050-snapshot/kml/generate-climate-layers.py` as a new Python script (separate from the existing generate-kml.py) that generates the Köppen classification layer.

    **Design:**
    
    The script opens `source/koppen_2041-2070_ssp370.tif` (the GloH2O V3 GeoTIFF) using `rasterio`, then:
    
    1. **Read raster:** Open the GeoTIFF, read band 1 (contains numeric Köppen class codes). If the file does not exist, print instructive error with download instructions from DISCOVERY.md and exit gracefully.
    
    2. **Polygonize:** Use `rasterio.features.shapes()` or GDAL `Polygonize()` to convert each contiguous zone of the same Köppen class into a polygon. Yield (polygon, value) pairs.
    
    3. **Classify:** Map numeric raster values to Köppen string codes using this legend (standard from Beck et al.):
       - 1=Af, 2=Am, 3=Aw, 4=BWh, 5=BWk, 6=BSh, 7=BSk, 8=Csa, 9=Csb, 10=Cwa, 11=Cwb, 12=Cwc, 13=Cfa, 14=Cfb, 15=Cfc, 16=Dsa, 17=Dsb, 18=Dsc, 19=Dwa, 20=Dwb, 21=Dwc, 22=Dfa, 23=Dfb, 24=Dfc, 25=Dfd, 26=ET, 27=EF
       (Verify exact legend from the GeoTIFF before hardcoding — the .tif may use a different value scheme)
    
    4. **Simplify geometries:** Apply `shapely.geometry.shape(polygon).simplify(0.02)` to reduce vertex count (matches project Douglas-Peucker convention from generate-kml.py D-16). Skip any polygon that simplifies to empty or has fewer than 4 coordinates.
    
    5. **Build KML hierarchy using simplekml:**
       ```python
       import simplekml
       kml = simplekml.Kml()
       koppen_folder = kml.newfolder(name="Köppen-Geiger Climate Classification (2050)")
       
       # Sub-folders per major climate group
       groups = {
           "A — Tropical Climates": ["Af", "Am", "Aw"],
           "B — Arid Climates": ["BWh", "BWk", "BSh", "BSk"],
           "C — Temperate Climates": ["Csa", "Csb", "Cwa", "Cwb", "Cwc", "Cfa", "Cfb", "Cfc"],
           "D — Continental Climates": ["Dsa", "Dsb", "Dsc", "Dwa", "Dwb", "Dwc", "Dfa", "Dfb", "Dfc", "Dfd"],
           "E — Polar Climates": ["ET", "EF"],
       }
       ```
       
    6. **Style per sub-type:** Each sub-type gets a dedicated style with:
       - PolyStyle color: AARRGGBB (80 + standard hex color, e.g., #0000FF → 800000FF)
       - LineStyle color: full opacity version (FF + hex)
       - Line width: 0.5 (matching existing overlay-zone style from entity-config.json)
       - Use `kml.newstyle()` or per-placemark style assignment
    
    7. **Cross-references:** Each placemark description includes:
       `See: 2050-snapshot/domains/climate.md#global-climate-state`
       For the Köppen layer specifically, add zone description text:
       `Köppen-Geiger climate zone: {subtype} ({full_name}) — 2050 projection under SSP3-7.0. See: 2050-snapshot/domains/climate.md#global-climate-state`
    
    8. **Output:** Write to `climate_koppen.kml` as a temporary output file. This file contains ONLY the Köppen folder. The merge into the main climate.kml will be handled in the next step.
    
    9. Script should log progress: how many polygons per sub-type, total vertex count, file sizes.
    
    **Full names for descriptions:**
    Af=Tropical Rainforest, Am=Tropical Monsoon, Aw=Tropical Savannah, BWh=Hot Desert, BWk=Cold Desert, BSh=Hot Semi-arid, BSk=Cold Semi-arid, Csa=Hot-summer Mediterranean, Csb=Warm-summer Mediterranean, Cwa=Monsoon humid subtropical, Cwb=Subtropical highland, Cwc=Cold subtropical highland, Cfa=Humid subtropical, Cfb=Oceanic, Cfc=Subpolar oceanic, Dsa=Mediterranean hot continental, Dsb=Mediterranean warm continental, Dsc=Mediterranean subarctic, Dwa=Monsoon hot continental, Dwb=Monsoon warm continental, Dwc=Monsoon subarctic, Dfa=Hot continental, Dfb=Warm continental, Dfc=Subarctic, Dfd=Extreme subarctic, ET=Tundra, EF=Frost/Ice Cap
  </action>
  <verify>
    <automated>python3 -c "import importlib; spec=importlib.util.spec_from_file_location('gen','2050-snapshot/kml/generate-climate-layers.py'); mod=importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert hasattr(mod, 'generate_koppen_kml') or callable(getattr(mod, 'main', None)); print('Script importable')"</automated>
  </verify>
  <done>generate-climate-layers.py exists with generate_koppen_kml() function and main() entry point, imports without error</done>
</task>

<task type="auto">
  <name>Task 2: Generate Köppen KML and merge into climate.kml</name>
  <files>2050-snapshot/kml/climate.kml</files>
  <action>
    Run the Köppen generation:
    
    1. Check if `source/koppen_2041-2070_ssp370.tif` exists. If not:
       - Print: "Köppen source GeoTIFF not found at source/koppen_2041-2070_ssp370.tif. Download from GloH2O V3 figshare (DOI 10.6084/m9.figshare.21789074) or run download-data.py from Plan 01"
       - Fall back to creating a demonstration layer using simplified continental-level polygon approximations from climate.md narrative (with NOTE comment marking as approximate)
       - The fallback uses the geographic descriptions in climate.md to create rough zone polygons (e.g., "tropical band 0-15° latitude", "arid band 15-35°")
    
    2. Run `python3 generate-climate-layers.py` to produce `climate_koppen.kml`
    
    3. Merge the new Köppen folder into `climate.kml`:
       - Parse existing climate.kml with lxml
       - Find the root `<Document>` element
       - Read the Köppen `<Folder>` from `climate_koppen.kml`
       - Insert it as the FIRST folder inside `<Document>` (before the existing "Climate" folder)
       - Preserve all existing styles, placemarks, and the existing "Climate" folder
       - Write back to climate.kml
    
    4. Verify:
       - climate.kml is valid XML (parse with lxml)
       - Köppen folder exists (`grep -c "Köppen-Geiger" climate.kml`)
       - At least 20 of 30 sub-types have polygons present
       - Total vertex count is reasonable (not exceeding 500K which would make Google Earth slow)
    
    5. If the GeoTIFF is absent and fallback was used, the merged climate.kml should still have a valid Köppen folder structure (even if approximate) with all 30 sub-type entries properly named and styled. The fallback polygons will be replaced when the real GeoTIFF becomes available.
    
    **Important:** Do NOT overwrite any existing content in climate.kml. Only ADD the new Köppen folder. The existing 11 thematic placemarks remain untouched.
  </action>
  <verify>
    <automated>python3 -c "
import lxml.etree as ET
tree = ET.parse('2050-snapshot/kml/climate.kml')
ns = {'kml': 'http://www.opengis.net/kml/2.2'}
folders = tree.findall('.//kml:Folder/kml:name', ns)
folder_names = [f.text for f in folders if f.text]
print(f'Folders: {folder_names}')
assert 'Köppen-Geiger Climate Classification (2050)' in folder_names, 'Köppen folder not found'
koppen_folder = [f for f in folders if f.text == 'Köppen-Geiger Climate Classification (2050)'][0]
parent = koppen_folder.getparent()
subfolders = parent.findall('kml:Folder', ns)
print(f'Sub-folders in Köppen: {len(subfolders)}')
placemarks = parent.findall('.//kml:Placemark', ns)
print(f'Total Köppen placemarks: {len(placemarks)}')
assert len(placemarks) > 50, 'Too few Köppen polygons'
print('PASS')
"</automated>
  </verify>
  <done>climate.kml contains a valid Köppen folder with all 30 sub-types polygonized and styled; existing Climate folder preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GloH2O GeoTIFF → polygonization | Raster data converted to vector geometry — potential data loss at simplification step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | D | Polygon simplification (0.02° threshold) | mitigate | Validate simplified polygons don't self-intersect via shapely.is_valid before writing KML |
| T-21-05 | I | Köppen class code legend mismatch | mitigate | Print histogram of raster values before mapping; use .tif .aux.xml metadata if available |
| T-21-06 | S | Large polygon count in KML | accept | D-03 accepts file size; Google Earth Pro handles large KML files natively |
</threat_model>

<verification>
1. `python3 generate-climate-layers.py` runs without error, produces `climate_koppen.kml`
2. Merged climate.kml parses as valid XML
3. Köppen folder has 5 sub-folders (A, B, C, D, E) with correctly named placemarks
4. Each sub-type has a distinct color from the standard palette
5. Description field contains `See:` back-reference to climate.md
6. Existing "Climate" folder with 11 thematic placemarks is untouched in climate.kml
7. Total Köppen polygon count remains within practical limits for Google Earth (< 5000 placemarks)
</verification>

<success_criteria>
- generate-climate-layers.py has a working `generate_koppen_kml()` function
- climate.kml has Köppen-Geiger folder as first child of Document
- All 30 Köppen sub-types present with correct styles and colors
- Cross-references point to climate.md#global-climate-state
- Existing 11 thematic placemarks preserved
- Fallback works gracefully if GeoTIFF not yet downloaded
</success_criteria>

<output>
After completion, create `.planning/phases/21-climate-kml-work/21-02-SUMMARY.md`
</output>
