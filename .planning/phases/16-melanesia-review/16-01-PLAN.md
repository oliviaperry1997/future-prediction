---
phase: 16-melanesia-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/borders.kml
autonomous: true
requirements:
  - OCEA-02
user_setup: []
must_haves:
  truths:
    - "borders.kml Melanesia folder renamed from 'Melanesia (wip)' to 'Melanesia' (line 65440)"
    - "entity-config.json Melanesia folder group renamed from 'Melanesia (wip)' to 'Melanesia' (line 267)"
    - "entity-config.json remains valid JSON after the rename"
    - "If Bougainville is added as a separate entity (D-04 decision): entity-config.json has a 'Bougainville' entry and borders.kml has a Bougainville folder — otherwise the 5-entity structure is unchanged"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Melanesia folder rename (and optional Bougainville entry)"
      contains: "\"Melanesia\":"
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Melanesia folder rename"
      contains: "<name>Melanesia</name>"
  key_links:
    - from: "entity-config.json"
      to: "borders.kml"
      via: "folder group name must match folder name"
      pattern: "Melanesia"
---

<objective>
Remove the `(wip)` tag from the Melanesia folder in both borders.kml and entity-config.json, completing the KML review status update for Phase 16.

Purpose: All 5 Melanesian entity polygons already exist (Fiji 44 placemarks, Kanaky 11, Papua New Guinea 58, Solomon Is. 48, Vanuatu 27). The `(wip)` tag signals the plausibility review is incomplete — this plan removes it after the review decision is made. Additionally, resolve D-04: determine whether Bougainville is a separate 6th entity based on synthesis of the transition doc PNG characterization.

Output: entity-config.json with Melanesia folder renamed; borders.kml with Melanesia folder renamed; optional Bougainville entry if D-04 resolves to independent entity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-melanesia-review/16-CONTEXT.md

<interfaces>
<!-- From 2050-snapshot/kml/entity-config.json — current Melanesia folder group (line 267) -->
From entity-config.json (lines 267-273 — the folder group to rename):
```json
"Melanesia (wip)": [
  "Fiji",
  "Kanaky",
  "Papua New Guinea",
  "Solomon Is.",
  "Vanuatu"
],
```
Rename to `"Melanesia"`.

<!-- From 2050-snapshot/kml/borders.kml — Melanesia folder (line 65440) -->
From borders.kml (line 65440 — the folder name to rename):
```xml
<name>Melanesia (wip)</name>
```
Rename to `<name>Melanesia</name>`.

<!-- Australasia pattern for comparison — entity-config.json folder group -->
From entity-config.json — the completed Australasia folder (what this should look like after rename):
```json
"Australasia": [
  "Australia",
  "New Zealand",
  "Christmas Island",
  ...
],
```

<!-- D-04 resolution: Bougainville independence determination -->
From 2026-2050-transition/regions/oceania.md (revolutionary/reactionary table):
- PNG is classified as a "bifurcation point" at Stage 1-2 (Not yet locked)
- "PNG follows the reactionary path by default — governance failure prevents it from capturing the revolutionary potential"
- "Bougainville achieves independence if PNG governance continues to fail and if the post-US environment demonstrates that small-state sovereignty is viable"

Decision logic for D-04: The transition doc's convergent evolution section explicitly states PNG follows the reactionary path by default. Under reactionary/failed PNG, the transition doc assigns ~2038-2042 Bougainville independence as likely. However, "LOW confidence" is flagged. The executor should read both the PNG bifurcation section (lines ~64-65) and the uncertainty ledger (line ~151) of oceania.md before making the D-04 call. The canonical answer flows from the PNG stage determination: if reactionary/failed → Bougainville likely independent by 2050; if revolutionary/functional → less likely.

<!-- If Bougainville IS added as a 6th entity, the entity-config.json entry pattern to follow: -->
From entity-config.json — the Kanaky entry (lines 1142+, the closest analog — small Pacific island entity):
```json
"Kanaky": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "NCL",
  ...
}
```
Bougainville would use country_code "BVL" (informal, not ISO — same pattern as other non-UN entities in the file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read transition doc, resolve D-04 (Bougainville), then rename Melanesia folder in both KML files</name>
  <files>2026-2050-transition/regions/oceania.md, 2050-snapshot/kml/entity-config.json, 2050-snapshot/kml/borders.kml</files>
  <read_first>
    - 2026-2050-transition/regions/oceania.md — lines 55-75 (Melanesia entity classifications), lines 100-130 (revolutionary/reactionary table for Fiji, PNG, Solomon Is.), lines 120-125 (Bougainville independence scenario), lines 148-155 (uncertainty ledger, LOW confidence flags)
    - 2050-snapshot/kml/entity-config.json — lines 265-275 (Melanesia folder group to rename), lines 1142-1165 (Kanaky entry — pattern for any Bougainville addition)
    - 2050-snapshot/kml/borders.kml — lines 65438-65445 (Melanesia folder name element)
  </read_first>
  <action>
**Step 1 — Resolve D-04 (Bougainville independence determination):**
Read oceania.md lines 55-75 and 100-155. The transition doc's convergent evolution section states: "PNG follows the reactionary path by default — governance failure prevents it from capturing the revolutionary potential of its resource wealth." Under this reactionary PNG outcome, the transition doc assigns Bougainville independence as likely (~2038-2042) if PNG governance continues to fail. This satisfies the D-04 condition. Canonical determination: **Bougainville is a separate 6th Melanesian entity in the 2050 snapshot** (assuming the reactionary PNG default per transition doc synthesis). Document this determination in the SUMMARY.

**Step 2 — Rename Melanesia folder in entity-config.json:**
In `2050-snapshot/kml/entity-config.json`, find the line `"Melanesia (wip)": [` (line ~267) and rename it to `"Melanesia": [`. If Bougainville is added (per Step 1), also add `"Bougainville"` to the array:
```json
"Melanesia": [
  "Fiji",
  "Kanaky",
  "Papua New Guinea",
  "Solomon Is.",
  "Vanuatu",
  "Bougainville"
],
```
Validate JSON after the edit: `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json'))"` — must exit 0.

**Step 3 — Add Bougainville entity entry in entity-config.json (if D-04 = independent):**
Find the existing `"Papua New Guinea"` entity entry in entity-config.json and add a `"Bougainville"` entry after it, following the same structure:
```json
"Bougainville": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "BVL",
  "folder_group": "Melanesia",
  "revolutionary_stage": "1-2",
  "status": "independent"
}
```
(Adapt fields to match the exact structure used by adjacent Pacific island entities in the file — Kanaky and Solomon Is. entries are the closest analogs.)

**Step 4 — Rename Melanesia folder in borders.kml:**
In `2050-snapshot/kml/borders.kml`, find the line `<name>Melanesia (wip)</name>` (line ~65440) and change it to `<name>Melanesia</name>`. This is a single string replacement — do not touch any other KML content.

**Step 5 — If Bougainville is a separate entity, add a Bougainville KML folder stub:**
After the Vanuatu subfolder closes inside the Melanesia folder, add a `<Folder>` element for Bougainville. The polygon extraction from PNG's polygon set is a research task for Plan 02 (the borders-geopolitics.md expansion will reference it). For now, create an empty placeholder:
```xml
<Folder>
  <name>Bougainville</name>
</Folder>
```
This satisfies D-09 as a structural placeholder. The actual polygon extraction from PNG's 58 placemarks is documented as a known gap to complete during Plan 02's analysis.
  </action>
  <verify>
    <automated>grep "Melanesia (wip)" 2050-snapshot/kml/entity-config.json 2050-snapshot/kml/borders.kml | wc -l | grep -q "^0$" && echo "PASS: no wip tags remain" || echo "FAIL: wip tag still present"</automated>
    <automated>grep -c "\"Melanesia\":" 2050-snapshot/kml/entity-config.json | grep -q "^1$" && echo "PASS: Melanesia key exists" || echo "FAIL"</automated>
    <automated>python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json')); print('PASS: JSON valid')"</automated>
    <automated>grep -c "<name>Melanesia</name>" 2050-snapshot/kml/borders.kml | grep -qE "^[1-9]" && echo "PASS: KML folder renamed" || echo "FAIL"</automated>
  </verify>
  <done>
    - entity-config.json has "Melanesia": [...] (no wip) with 5 or 6 entities depending on D-04 resolution
    - borders.kml has &lt;name&gt;Melanesia&lt;/name&gt; (no wip)
    - JSON remains valid
    - D-04 determination documented in SUMMARY (Bougainville: independent or part of PNG)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan edits static content files (KML, JSON). No user input, no external services, no code execution paths, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | entity-config.json | accept | JSON validation command (`python3 -c "import json; json.load(...)"`) catches malformed edits before commit. Low-risk static content edit. |
| T-16-02 | Tampering | borders.kml | accept | Single string replacement; KML is not machine-parsed at runtime. Manual review of KML in Google Earth Pro catches visual regressions. |
</threat_model>

<verification>
- `grep "Melanesia (wip)" 2050-snapshot/kml/entity-config.json` → no output
- `grep "Melanesia (wip)" 2050-snapshot/kml/borders.kml` → no output
- `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json'))"` → exits 0
- SUMMARY records D-04 determination (Bougainville entity status)
</verification>

<success_criteria>
Both KML files have Melanesia folder renamed from "(wip)" to clean "Melanesia". entity-config.json is valid JSON. D-04 (Bougainville) is resolved and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/16-melanesia-review/16-01-SUMMARY.md`
</output>
