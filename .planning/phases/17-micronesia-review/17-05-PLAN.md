---
phase: 17-micronesia-review
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/user_colors.json
autonomous: true
gap_closure: true
requirements:
  - OCEA-03
user_setup: []
must_haves:
  truths:
    - "Guam entity-config.json section_anchor and see_path are consistent (both reference 'guam')"
    - "Guam Placemark description in borders.kml points to #guam (not stale #guam-hfs-compact)"
    - "CNMI has a KML polygon in borders.kml inside the Micronesia folder"
    - "CNMI has a color entry in user_colors.json"
    - "KML file remains valid after edits — passes `python3 -c \"from lxml import etree; etree.parse(...)\"`"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Consistent Guam section_anchor/see_path"
      contains: '"section_anchor": "guam"' AND '"see_path": "...#guam"'
    - path: "2050-snapshot/kml/borders.kml"
      provides: "CNMI KML polygon + fixed Guam description anchor"
      contains: "<name>CNMI</name>"
    - path: "2050-snapshot/kml/user_colors.json"
      provides: "CNMI color entry"
      contains: '"CNMI":'
  key_links:
    - from: "entity-config.json Guam section_anchor"
      to: "borders.kml Guam Placemark description"
      via: "consistent 'guam' anchor"
      pattern: "guam"
    - from: "domain docs CNMI entries"
      to: "borders.kml CNMI Placemark"
      via: "new CNMI polygon now resolves stale → See KML: CNMI references"
      pattern: "CNMI"
---

<objective>
Close two remaining gaps from Phase 17 code review: (1) fix Guam entity-config.json section_anchor/see_path inconsistency (IN-02), and (2) add CNMI KML polygon to borders.kml so domain doc → See KML: CNMI references resolve to an actual polygon (WR-04).

Purpose: Eliminate all code review findings for Phase 17 by fixing the two outstanding issues — Guam's stale #guam-hfs-compact anchor and CNMI's missing polygon.
Output: Consistent entity-config.json + borders.kml with CNMI polygon in Micronesia folder + CNMI color in user_colors.json.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
Existing Guam entry in entity-config.json (lines 1820-1828):
{"Guam": {"type": "entity", "category": "global", "source": "country", "country_code": "GUM", "section_anchor": "guam", "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#guam-hfs-compact"}}

All other Micronesian entities have matching section_anchor/see_path:
- Kiribati: "kiribati" / "#kiribati"
- Marshall Is.: "marshall-islands" / "#marshall-islands"
- Micronesia FSM: "micronesia-fsm" / "#micronesia-fsm"
- Nauru: "nauru" / "#nauru"
- Palau: "palau" / "#palau"
- CNMI: "cnmi" / "#cnmi"

Micronesia KML folder structure in borders.kml (lines 67724-68801):
```
<Folder>           <!-- line 67724 — Oceania sub-folder -->
  <name>Micronesia</name>  <!-- line 67725 -->
  <Folder><name>Guam</name>...</Folder>          <!-- lines 67727-67741 -->
  <Folder><name>Kiribati</name>...</Folder>      <!-- lines 67742+ -->
  <Folder><name>Marshall Is.</name>...</Folder>
  <Folder><name>Micronesia</name>...</Folder>    <!-- FSM — line 68579+ -->
  <Folder><name>Nauru</name>...</Folder>         <!-- line 68676+ -->
  <Folder><name>Palau</name>...</Folder>          <!-- line 68691+ -->
</Folder>          <!-- line 68801 — closes Micronesia -->
```

CNMI source data: N. Mariana Is. in global-countries-10m.kml (ADM0_A3=MNP) — 12 polygons representing the Mariana Islands chain, ~109 total coordinate points.
</interfaces>

See also code review findings:
- `.planning/phases/17-micronesia-review/17-REVIEW.md` — IN-02 (Guam mismatch), WR-04 (CNMI polygon missing)
- `2050-snapshot/kml/entity-config.json` — Guam entry at line 1820
- `2050-snapshot/kml/borders.kml` — Micronesia folder at line 67724
- `2050-snapshot/kml/user_colors.json` — Existing Micronesian entity colors
- `2050-snapshot/kml/source/global-countries-10m.kml` — N. Mariana Is. (MNP) polygon source

</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Guam section_anchor/see_path consistency in entity-config.json + borders.kml</name>
  <files>
    2050-snapshot/kml/entity-config.json
    2050-snapshot/kml/borders.kml
  </files>
  <action>
    Fix two inconsistencies where Guam references the stale "guam-hfs-compact" anchor instead of "guam":

    1. **entity-config.json line 1827:** Change `"see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#guam-hfs-compact"` to `"see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#guam"`.

    2. **borders.kml line 67731:** Change `<description>See: 2050-snapshot/domains/borders-geopolitics.md#guam-hfs-compact</description>` to `<description>See: 2050-snapshot/domains/borders-geopolitics.md#guam</description>`.

    Do NOT change the `section_anchor` value — `"guam"` is the correct anchor name. The see_path was stale (referencing the old "HFS" Hawaiian Federation of States naming that predates the Pacifica/HFS compact terminology).
  </action>
  <verify>
    <automated>grep -c 'guam-hfs-compact' 2050-snapshot/kml/entity-config.json 2050-snapshot/kml/borders.kml</automated>
  </verify>
  <done>Both files have zero references to "guam-hfs-compact". entity-config.json Guam see_path uses "#guam". borders.kml Guam description uses "#guam".</done>
</task>

<task type="auto">
  <name>Task 2: Add CNMI KML polygon to borders.kml + CNMI color to user_colors.json</name>
  <files>
    2050-snapshot/kml/user_colors.json
    2050-snapshot/kml/borders.kml
  </files>
  <action>
    The N. Mariana Is. (ISO_A3=MNP) polygon data exists in global-countries-10m.kml — 12 small polygons representing the Mariana Islands chain (~109 total coordinate points). Extract and insert:

    ### Part A: Add CNMI color to user_colors.json

    Add a CNMI color entry with a distinct Pacific-island hue. Use a color that differentiates CNMI from Guam (white #ffffff) and the other Micronesian entities. Suggested: a coral/teal shade, e.g. `#ff7f50` (Coral) or `#20b2aa` (LightSeaGreen). The user may prefer a specific color — use reasonable judgment:
    ```json
    "CNMI": {
      "hex": "#3cb371",
      "kml_fill": "8071b33c",
      "kml_line": "ff71b33c",
      "width": 1.0
    }
    ```
    (MediumSeaGreen — distinct from Guam's white, Kiribati's purple, Marshall Is.'s red, FSM's green, Nauru's dark brown, Palau's gold. Similar hue family as FSM but different shade since they're adjacent in the islands chain.)

    Use the natural alphabetical position among Micronesian entities: insert between "Chile" and "China" (or whichever alphabetically adjacent entries exist in user_colors.json). Maintain alphabetical ordering by entity name.

    ### Part B: Add CNMI Placemark to borders.kml

    Write and run a Python script at `2050-snapshot/kml/add-cnmi-polygon.py` to extract the N. Mariana Is. polygon from the source KML and insert it into borders.kml. The script should:

    1. Load `2050-snapshot/kml/source/global-countries-10m.kml` and find the Placemark named "N. Mariana Is." (ADM0_A3=MNP).
    2. Extract all 12 Polygon elements from within the MultiGeometry. Each polygon has an `<outerBoundaryIs>` with a `<coordinates>` element.
    3. Read `2050-snapshot/kml/borders.kml` and find the `<name>Micronesia</name>` folder (line ~67725).
    4. Clone the structure of the existing Guam Placemark (lines 67727-67741) but using:
       - Folder name: "CNMI"
       - Placemark name: "CNMI"
       - Description: "See: 2050-snapshot/domains/borders-geopolitics.md#cnmi"
       - All 12 polygon coordinates from the source data, wrapped in a `<MultiGeometry>` element.
       - Use a generated styleUrl: scan borders.kml for any existing `#__managed_style_*` IDs and generate one that doesn't collide (e.g., increment from the highest existing number).
    5. Insert the new `<Folder><name>CNMI</name>...</Folder>` as the **first** sub-folder inside the Micronesia folder (right after the `<name>Micronesia</name>` line, before the `<Folder><name>Guam</name>`). Or as the **last** before the closing `</Folder>` — either is fine. First is preferred (alphabetical).
    6. Write updated borders.kml back to disk.
    7. Delete the add-cnmi-polygon.py helper script after successful insertion.

    The resulting Placemark structure should follow the exact pattern of existing entities:
    ```xml
    <Folder>
      <name>CNMI</name>
      <Placemark>
        <name>CNMI</name>
        <styleUrl>#__managed_style_XXXXXXXXXXXXXXX</styleUrl>
        <description>See: 2050-snapshot/domains/borders-geopolitics.md#cnmi</description>
        <MultiGeometry>
          <Polygon>
            <altitudeMode>clampToGround</altitudeMode>
            <outerBoundaryIs>
              <LinearRing>
                <coordinates>...</coordinates>
              </LinearRing>
            </outerBoundaryIs>
          </Polygon>
          <!-- repeat for each of the 12 sub-polygons -->
        </MultiGeometry>
      </Placemark>
    </Folder>
    ```

    Use the style format matching existing entries (gx:CascadingStyle for the style definitions, StyleMap for normal/highlight pairs). The style from user_colors.json will be picked up by the existing style-generation pattern in borders.kml — but since this is a manual insert (not a regeneration), the Placemark needs a styleUrl that matches a manually defined cascading style.

    IMPORTANT: Since the generate-kml.py script is NOT being re-run (it would overwrite all other manual edits), this insertion must be done manually via the Python script. Do NOT regenerate borders.kml from scratch.

    **Validate:** After insertion, run `python3 -c "from lxml import etree; etree.parse('2050-snapshot/kml/borders.kml')"` to confirm XML validity.
  </action>
  <verify>
    <automated>python3 -c "from lxml import etree; etree.parse('2050-snapshot/kml/borders.kml')" && grep -c '<name>CNMI</name>' 2050-snapshot/kml/borders.kml && python3 -c "import json; d=json.load(open('2050-snapshot/kml/user_colors.json')); assert 'CNMI' in d, 'CNMI color missing'"</automated>
  </verify>
  <done>CNMI Placemark with all 12 island polygons exists inside the Micronesia folder in borders.kml. CNMI color entry exists in user_colors.json. KML file parses as valid XML. All domain doc "→ See KML: CNMI" references now point to a real polygon.</done>
</task>

</tasks>

<verification>
- `grep -c 'guam-hfs-compact' entity-config.json borders.kml` → 0: stale anchor removed
- `python3 -c "from lxml import etree; etree.parse('borders.kml')"` → exits 0: valid XML
- `grep -c '<name>CNMI</name>' borders.kml` → 1+: CNMI Placemark present
- `python3 -c "import json; json.load(open('user_colors.json'))['CNMI']"` → exits 0: CNMI color present
</verification>

<success_criteria>
1. All 17-REVIEW.md warnings resolved: WR-01 (already fixed), WR-02 (already fixed), WR-03 (already fixed), WR-04 (fixed — CNMI polygon now exists)
2. All 17-REVIEW.md info items addressed: IN-01 (already fixed), IN-02 (fixed — Guam anchor consistent)
3. Borders.kml XML validates without errors
4. User_colors.json has CNMI entry
5. Phase 17 has zero open code review findings
</success_criteria>

<output>
After completion, create `.planning/phases/17-micronesia-review/17-05-SUMMARY.md`
</output>
