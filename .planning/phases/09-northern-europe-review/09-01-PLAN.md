---
phase: 09-northern-europe-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/borders.kml
autonomous: true
requirements:
  - EURA-04
user_setup: []
must_haves:
  truths:
    - "Eurasia Northern Europe KML folder renamed (no wip tag)"
    - "Northern Europe folder_hierarchy in entity-config.json lists only United Kingdom"
    - "Iceland, Norway, Ireland entity entries removed from entity-config.json"
    - "NOR and ISL country codes added to European Federation country_codes array"
    - "Leftover EU member entity entries (Germany, Spain, Portugal, Italy, Austria, Greece, Croatia, Cyprus, Malta) removed from entity-config.json"
    - "United Kingdom entity entry updated with late-revolutionary classification and Scotland departure note"
    - "Åland Islands placemark added as KML sub-entity within EU Nordic zone"
    - "borders.kml Northern Europe folder contains only United Kingdom — no Iceland, Norway, or individual EU member folders"
    - "borders.kml European Federation folder contains Iceland and Norway polygons"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Updated entity config: Northern Europe = [United Kingdom]; Iceland, Norway, Ireland removed; NOR+ISL added to European Federation; leftover EU member entities removed; UK entry updated; Åland entry added"
      contains: "Northern Europe folder_hierarchy: [United Kingdom]; European Federation country_codes includes NOR, ISL; no Iceland, Norway, Ireland, Germany, Spain, Portugal, Italy, Austria, Greece, Croatia, Cyprus, Malta entities"
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Updated borders.kml with Northern Europe restructuring: UK-only folder, Iceland/Norway polygons in European Federation, Åland placemark, no wip tag"
      contains: "Northern Europe folder with only United Kingdom; European Federation folder includes Iceland and Norway polygons; Åland Islands Placemark; no (wip) tag"
  key_links:
    - from: "entity-config.json::folder_hierarchy Northern Europe"
      to: "entity-config.json::entities United Kingdom"
      pattern: "folder_hierarchy references only United Kingdom"
    - from: "borders.kml::European Federation Placemark descriptions"
      to: "borders-geopolitics.md#european-federation"
      pattern: "See: 2050-snapshot/domains/borders-geopolitics.md#european-federation"
    - from: "borders.kml::United Kingdom Placemark descriptions"
      to: "borders-geopolitics.md#united-kingdom"
      pattern: "See: 2050-snapshot/domains/borders-geopolitics.md#united-kingdom"
---

<objective>
Update entity-config.json and borders.kml for Northern Europe: remove (wip) tags, restructure the Northern Europe KML folder for the 2050 geopolitical reality (all Northern EU members merged into European Federation, UK as the sole non-EU entity), add NOR and ISL to European Federation country_codes, remove Iceland/Norway/Ireland and leftover EU member entity entries, add Åland Islands as a visible sub-entity, and update the UK entry. Per D-14 through D-17.

Purpose: Northern Europe's KML representation must reflect the 2050 reality: a federal EU that has absorbed Iceland, Norway, and Scotland (via Ireland); a UK in early-stage revolutionary status post-late-flip; and an independent Åland sub-entity visible on the map. The (wip) tag removal signals Northern Europe as reviewed.
Output: Updated `entity-config.json` and `borders.kml` with corrected Northern Europe representation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-northern-europe-review/09-CONTEXT.md
@.planning/phases/08-eastern-europe-review/08-01-PLAN.md

<interfaces>
**Current entity-config.json — Eurasia folder_hierarchy (Northern Europe):**
```json
"Northern Europe (wip)": [
  "Iceland",
  "Norway",
  "United Kingdom"
],
```

**Current entity-config.json — Northern Europe entity entries that need removal:**
- Iceland: {source: "country", country_code: "ISL", section_anchor: "", see_path: "..."}
- Norway: {source: "country", country_code: "NOR", section_anchor: "", see_path: "..."}
- Ireland: {source: "country", country_code: "IRL", section_anchor: "", see_path: "..."}

**Leftover EU member entity entries (Phase 8 incomplete cleanup) needing removal:**
- Germany: {source: "country", country_code: "DEU", section_anchor: "germany", see_path: "...#germany"}
- Spain: {source: "country", country_code: "ESP", section_anchor: "", see_path: "..."}
- Portugal: {source: "country", country_code: "PRT", section_anchor: "", see_path: "..."}
- Italy: {source: "country", country_code: "ITA", section_anchor: "", see_path: "..."}
- Austria: {source: "country", country_code: "AUT", section_anchor: "", see_path: "..."}
- Greece: {source: "country", country_code: "GRC", section_anchor: "", see_path: "..."}
- Croatia: {source: "country", country_code: "HRV", section_anchor: "", see_path: "..."}
- Cyprus: {source: "country", country_code: "CYP", section_anchor: "", see_path: "..."}
- Malta: {source: "country", country_code: "MLT", section_anchor: "", see_path: "..."}

**Current European Federation entity:**
```json
"European Federation": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "country_codes": ["POL", "CZE", "SVK", "HUN", "ROU", "BGR", "DNK", "EST", "FIN", "IRL", "LVA", "LTU", "SWE", "AUT", "BEL", "FRA", "DEU", "LUX", "NLD", "HRV", "CYP", "GRC", "ITA", "MLT", "PRT", "SVN", "ESP", "MDA", "ATF"],
  "preserve_holes": true,
  "min_hole_area": 0.01,
  "unify_buffer": 0.002,
  "subtract_admin1_per_code": { ... },
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "european-union",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#european-union"
}
```

**Current United Kingdom entity:**
```json
"United Kingdom": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "country_codes": ["GBR", "SHN"],
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "united-kingdom",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#united-kingdom"
}
```

**Current borders.kml — Northern Europe folder (starts at line ~51470):**
```
<Folder>
  <name>Northern Europe (wip)</name>
  <Folder><name>Iceland</name>... (~2 Placemarks)</Folder>
  <Folder><name>Norway</name>... (many Placemarks)</Folder>
  <Folder><name>United Kingdom</name>... (GBR + Saint Helena Placemarks)</Folder>
</Folder>
```

Iceland has ~2 Placemarks (mainland polygons). Norway has many Placemarks (mainland + Svalbard + Jan Mayen etc.). UK has GBR Placemarks + Saint Helena.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update entity-config.json — remove Iceland/Norway/Ireland/leftover EU members, add NOR+ISL to European Federation, update UK, add Åland, fix folder_hierarchy</name>
  <files>2050-snapshot/kml/entity-config.json</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (folder_hierarchy Eurasia section, European Federation entry, all entity entries that need removal, UK entry)
  </read_first>
  <action>
    Edit `2050-snapshot/kml/entity-config.json`:

    **A. Remove (wip) from Northern Europe subregion key in folder_hierarchy:**
    Change `"Northern Europe (wip)"` → `"Northern Europe"`. 
    Do NOT touch any other (wip) regions (Southeast Asia, Southern Asia, Southern Europe, Western Asia — other phases handle them).

    **B. Update Northern Europe folder_hierarchy entry** to UK-only:
    ```json
    "Northern Europe": [
      "United Kingdom"
    ],
    ```
    Per D-15: "Northern Europe KML folder retains only United Kingdom as a non-EU entity. All other Northern European entities are EU subdivisions."

    **C. Add NOR and ISL to European Federation country_codes:**
    Find the European Federation entity's `country_codes` array. Insert `"NOR"` and `"ISL"` into the array (alphabetically between "NLD" and "POL"):
    ```json
    "country_codes": ["POL", "CZE", ..., "NLD", "NOR", "ISL", "POL", ...]
    ```
    Wait — alphabetically: ... MDA, MLT, NLD, NOR, NPL (wait, NOR comes after NLD). Let me verify actual alphabetical order:
    Current: POL, CZE, SVK, HUN, ROU, BGR, DNK, EST, FIN, IRL, LVA, LTU, SWE, AUT, BEL, FRA, DEU, LUX, NLD, HRV, CYP, GRC, ITA, MLT, PRT, SVN, ESP, MDA, ATF
    NOR goes after NLD. ISL goes after IRL (wait, IRL is already there) — actually ISL goes after... let me think: DNK, EST, FIN, IRL, ISL, LVA, LTU... Yes, ISL goes between IRL and LVA.
    
    Insert `"ISL"` after `"IRL"` and `"NOR"` after `"NLD"`:
    ```json
    "country_codes": ["POL", "CZE", "SVK", "HUN", "ROU", "BGR", "DNK", "EST", "FIN", "IRL", "ISL", "LVA", "LTU", "SWE", "AUT", "BEL", "FRA", "DEU", "LUX", "NLD", "NOR", "HRV", "CYP", "GRC", "ITA", "MLT", "PRT", "SVN", "ESP", "MDA", "ATF"],
    ```
    Per D-01 (Norway joins EU), D-02 (Iceland joins EU).

    **D. Remove individual Northern European entity entries** — Delete these entity entries entirely (including trailing commas):
    - `"Iceland": { ... },`
    - `"Norway": { ... },`
    - `"Ireland": { ... },`
    Per D-02, D-12, D-15.

    **E. Remove leftover EU member entity entries** from pre-Phase 8 that should have been cleaned up — Delete entirely:
    - `"Germany": { ... },`
    - `"Spain": { ... },`
    - `"Portugal": { ... },`
    - `"Italy": { ... },`
    - `"Austria": { ... },`
    - `"Greece": { ... },`
    - `"Croatia": { ... },`
    - `"Cyprus": { ... },`
    - `"Malta": { ... },`
    Per D-16: "Remove individual entry for Spain, Portugal, Italy, Austria, Greece, Croatia etc. (Phase 8 leftover cleanup)."

    **F. Update United Kingdom entity entry** — per D-08, D-09, D-16:
    Modify the existing UK entry to include the late-revolutionary classification and note Scotland's departure:
    ```json
    "United Kingdom": {
      "type": "entity",
      "category": "global",
      "source": "group",
      "keep_unified": true,
      "country_codes": ["GBR", "SHN"],
      "classification": "revolutionary (late flip ~2045-2048)",
      "notes": "Scotland exited ~2035-2038 to join EU; Isle of Man, Channel Islands remain as Crown Dependencies",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "united-kingdom",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#united-kingdom"
    },
    ```
    The `classification` and `notes` fields are informational for downstream agents. The `country_codes` remain `["GBR", "SHN"]` (Great Britain + Saint Helena). Isle of Man and Channel Islands are Crown Dependencies — no separate ISO codes in Natural Earth data.

    **G. Add Åland Islands entry** — per D-04:
    Add a new entity entry for Åland Islands. Insert it in the entities section at an appropriate location (alphabetically between Austria and Azerbaijan or wherever fits the entity listing):
    ```json
    "Åland Islands": {
      "type": "entity",
      "category": "subnational",
      "source": "placemark",
      "country_code": "ALA",
      "parent_entity": "European Federation",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "european-union",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#european-union"
    },
    ```
    Since Åland is a sub-entity placemark within the EU, use the domain_doc reference pointing to the European Federation section.

    **Important:** Maintain valid JSON. After all edits, verify with `python3 -m json.tool`.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)
# Check EU entity has NOR and ISL
eu = d['entities']['European Federation']
codes = eu['country_codes']
assert 'NOR' in codes, 'NOR missing from EU codes'
assert 'ISL' in codes, 'ISL missing from EU codes'
assert len(codes) == 31, f'EU has {len(codes)} codes, expected 31'
# Check removed entities
removed = ['Iceland','Norway','Ireland','Germany','Spain','Portugal','Italy','Austria','Greece','Croatia','Cyprus','Malta']
for r in removed:
    assert r not in d['entities'], f'{r} still in entities'
# Check retained entities  
assert 'United Kingdom' in d['entities'], 'United Kingdom missing'
# Check folder hierarchy
ne = d['folder_hierarchy']['Eurasia']['Northern Europe']
assert ne == ['United Kingdom'], f'Northern Europe folder wrong: {ne}'
assert '(wip)' not in str(d['folder_hierarchy']['Eurasia']), 'wip tag still present'
# Check Åland
assert 'Åland Islands' in d['entities'] or 'Åland' in d['entities'], 'Åland Islands missing'
print('PASS: all entity-config checks passed')
"</automated>
  </verify>
  <done>
    entity-config.json: Iceland, Norway, Ireland, Germany, Spain, Portugal, Italy, Austria, Greece, Croatia, Cyprus, Malta entries removed. NOR and ISL added to European Federation country_codes (now 31). UK entry updated with late-revolutionary classification and Scotland departure note. Åland Islands added as sub-entity. Northern Europe folder_hierarchy updated to ['United Kingdom']. Per D-14 through D-17.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit borders.kml — remove (wip), restructure Northern Europe to UK-only, move Iceland/Norway polygons into European Federation, add Åland placemark</name>
  <files>2050-snapshot/kml/borders.kml</files>
  <read_first>
    - 2050-snapshot/kml/borders.kml (Northern Europe folder starts at line ~51470 — find the opening `<Folder><name>Northern Europe (wip)</name>` and the closing `</Folder>` for the Northern Europe region)
    - 2050-snapshot/kml/borders.kml (European Federation folder — find where it is in the KML to identify where Iceland/Norway polygons should be added)
  </read_first>
  <action>
    Edit `2050-snapshot/kml/borders.kml` within and around the Northern Europe folder:

    **A. Remove (wip) tag from folder name:**
    Change: `<name>Northern Europe (wip)</name>` → `<name>Northern Europe</name>`
    Per D-14.

    **B. Move Iceland <Folder> contents into European Federation folder:**
    The Northern Europe folder currently contains:
    ```
    <Folder><name>Iceland</name>... (~2 Placemarks)</Folder>
    <Folder><name>Norway</name>... (many Placemarks + Svalbard)</Folder>
    <Folder><name>United Kingdom</name>... (GBR + SHN Placemarks)</Folder>
    ```

    Find the European Federation folder elsewhere in borders.kml (it should exist from Phase 8's Eastern Europe restructuring — search for `<name>European Federation</name>` or similar). It may be named "European Union" or "European Federation" in the KML.

    For each Placemark being moved from Iceland/Norway folders into the European Federation folder:
    - Keep the `<name>` as-is
    - Keep the `<styleUrl>` as-is
    - Update the `<description>` to: `<description>See: 2050-snapshot/domains/borders-geopolitics.md#european-federation</description>`
    - Insert the Placemark inside the European Federation folder alongside existing EU member Placemarks

    Delete the now-empty Iceland and Norway folders:
    - Remove the entire `<Folder><name>Iceland</name>...Placemarks...</Folder>` block
    - Remove the entire `<Folder><name>Norway</name>...Placemarks...</Folder>` block

    Per D-01, D-02, D-15.

    **C. Update United Kingdom Placemark descriptions:**
    Inside the remaining `<Folder><name>United Kingdom</name>`, update all Placemark descriptions to add anchor:
    Current: `<description>See: 2050-snapshot/domains/borders-geopolitics.md</description>`
    Change to: `<description>See: 2050-snapshot/domains/borders-geopolitics.md#united-kingdom</description>`
    Per D-08.

    **D. Add Åland Islands Placemark inside the European Federation folder:**
    Inside the European Federation folder, add a new Placemark for Åland Islands. Since Åland is a natural feature, create a simple point placemark (Google Earth will show it as a label/pin on the map):
    ```xml
    <Placemark>
      <name>Åland Islands</name>
      <styleUrl>#__managed_style_000000000000012F</styleUrl>
      <description>See: 2050-snapshot/domains/borders-geopolitics.md#european-federation</description>
      <Point>
        <coordinates>19.95,60.25,0</coordinates>
      </Point>
    </Placemark>
    ```
    Åland is at approximately 60.25°N, 19.95°E (Mariehamn area). Per D-04: "Åland Islands get a separate KML polygon/placemark within the EU Nordic zone."

    **E. Verify original placemark counts:**
    Before deleting any folders, count the Iceland and Norway Placemarks. After moving, verify the European Federation folder has gained exactly that many Placemarks and the Iceland/Norway folders no longer exist.

    **F. Add a `<!-- Note: ... -->` XML comment inside the Northern Europe folder:**
    ```xml
    <!-- Note: Northern Europe reviewed Phase 9 (2026-05-28). All entities except United Kingdom merged into European Federation per D-15. Åland Islands added as sub-entity per D-04. Svalbard, Faroe Islands follow EU status — see CONTEXT.md D-05, D-06. -->
    ```

    **Important:**
    - Do NOT modify any Placemarks outside the Northern Europe folder
    - Do NOT modify the United Kingdom or European Federation folders in any way other than the Placemark moves described above
    - Verify the KML is well-formed XML after all edits (balanced Folder and Placemark tags)
    - This is a large file (~5.5MB) — use targeted find-replace within the identified line ranges
  </action>
  <verify>
    <automated>echo "=== (wip) check ===" && grep -c 'Northern Europe (wip)' 2050-snapshot/kml/borders.kml && echo "(should be 0)" && echo "=== Northern Europe folder exists ===" && grep -c '<name>Northern Europe</name>' 2050-snapshot/kml/borders.kml && echo "(should be >=1)" && echo "=== Iceland folder removed ===" && for c in Iceland Norway; do echo -n "$c: "; grep -c "<name>$c</name>" 2050-snapshot/kml/borders.kml; done && echo "(Iceland should be 0 if no Placemarks left, Norway similar)" && echo "=== UK still exists ===" && grep -c '<name>United Kingdom</name>' 2050-snapshot/kml/borders.kml && echo "(should be >=1)" && echo "=== Åland placemark ===" && grep -c 'Åland Islands' 2050-snapshot/kml/borders.kml && echo "(should be >=1)" && echo "=== Description anchor ===" && grep -c 'borders-geopolitics.md#united-kingdom' 2050-snapshot/kml/borders.kml</automated>
  </verify>
  <done>
    borders.kml: Northern Europe folder has no (wip) tag. Iceland and Norway Placemarks moved into European Federation folder with updated description anchors. Northern Europe folder contains only United Kingdom folder. Åland Islands Placemark added inside European Federation. UK Placemark descriptions updated with #united-kingdom anchor. Per D-14 through D-17.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify KML well-formedness and validate entity config integrity</name>
  <files>2050-snapshot/kml/borders.kml, 2050-snapshot/kml/entity-config.json</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (post-edit, re-read for verification)
    - 2050-snapshot/kml/borders.kml (post-edit, re-read key sections for verification)
  </read_first>
  <action>
    Final validation checks:

    **1. Validate entity-config.json is valid JSON:**
    ```bash
    python3 -m json.tool 2050-snapshot/kml/entity-config.json > /dev/null && echo "JSON valid"
    ```

    **2. Verify borders.kml structural integrity:**
    ```bash
    python3 -c "
    with open('2050-snapshot/kml/borders.kml') as f:
        content = f.read()
    # Check key folder names
    for name in ['Northern Europe', 'European Federation', 'United Kingdom']:
        count = content.count(f'<name>{name}</name>')
        print(f'{name}: {count} occurrence(s)')
    # Verify Iceland/Norway no longer separate folders
    for name in ['Iceland', 'Norway']:
        count = content.count(f'<name>{name}</name>')
        if count > 0:
            # Check if these are Placemark names (OK) or Folder names (bad)
            folder_count = content.count(f'<Folder><name>{name}</name>')
            print(f'{name} folders: {folder_count} (should be 0 if fully merged)')
        else:
            print(f'{name}: not found directly')
    # Verify Åland exists
    print(f'Åland: {\"present\" if \"Åland\" in content else \"missing\"}')
    print('Structural check complete')
    "
    ```

    **3. Verify entity-config.json folder-entity consistency:**
    ```bash
    python3 -c "
    import json
    with open('2050-snapshot/kml/entity-config.json') as f:
        d = json.load(f)
    ne_folder = d['folder_hierarchy']['Eurasia']['Northern Europe']
    for name in ne_folder:
        assert name in d['entities'], f'{name} in folder_hierarchy but not in entities'
    print(f'Northern Europe folder: {ne_folder}')
    # Check European Federation has NOR and ISL
    eu = d['entities']['European Federation']
    assert 'NOR' in eu['country_codes'], 'NOR missing'
    assert 'ISL' in eu['country_codes'], 'ISL missing'
    print(f'EU country_codes count: {len(eu[\"country_codes\"])}')
    # Check removed entities
    removed = ['Iceland', 'Norway', 'Ireland', 'Germany', 'Spain', 'Portugal', 'Italy', 'Austria', 'Greece', 'Croatia', 'Cyprus', 'Malta']
    for r in removed:
        assert r not in d['entities'], f'{r} STILL in entities!'
    print(f'All {len(removed)} entities verified as removed')
    print('All consistency checks passed')
    "
    ```

    **4. Check that generate-kml.py can parse the updated config:**
    ```bash
    python3 -c "
    import json
    with open('2050-snapshot/kml/entity-config.json') as f:
        d = json.load(f)
    assert 'European Federation' in d['entities']
    assert 'United Kingdom' in d['entities']
    print('Pipeline config parse OK')
    "
    ```
  </action>
  <verify>
    <automated>python3 -c "
import json
# JSON validation
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)
assert 'European Federation' in d['entities']
eu = d['entities']['European Federation']
assert 'NOR' in eu['country_codes'], 'NOR missing'
assert 'ISL' in eu['country_codes'], 'ISL missing'
assert 'United Kingdom' in d['entities']
assert 'Northern Europe' in d['folder_hierarchy']['Eurasia']
assert d['folder_hierarchy']['Eurasia']['Northern Europe'] == ['United Kingdom']
assert '(wip)' not in str(d['folder_hierarchy']['Eurasia'])
# KML checks
with open('2050-snapshot/kml/borders.kml') as f:
    kml = f.read()
assert '<name>Northern Europe (wip)</name>' not in kml, 'wip still present'
assert '<name>Northern Europe</name>' in kml, 'Northern Europe folder missing'
print('PASS: All final validation checks passed')
"</automated>
  </verify>
  <done>entity-config.json is valid JSON. borders.kml is structurally intact. Northern Europe folder hierarchy is consistent between config and KML. European Federation includes NOR and ISL. Removed entities confirmed absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KML file edit | Local file modification — no external input, no untrusted data |
| JSON file edit | Local file modification — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | D | JSON parse error from entity-config.json edits | mitigate | Validate JSON with `python3 -m json.tool` after all edits. Verify all required entity entries exist and folder_hierarchy keys match entity names. |
| T-09-02 | D | Accidental deletion of United Kingdom KML polygons during Iceland/Norway move operation | mitigate | Explicitly exclude United Kingdom folder from any removal operations. Verify UK folder and Placemarks present after edits. |
| T-09-03 | D | KML XML structural damage — mismatched Folder/Placemark tags from cut-paste operations | mitigate | Verify Folder open/close balance in the Northern Europe and European Federation regions. |
| T-09-04 | D | entity-config.json folder_hierarchy references United Kingdom but UK entry has stale/inconsistent data | mitigate | Verify United Kingdom entry has classification, notes, and proper see_path after updates. |
| T-09-05 | I | Entity config `country_codes` for European Federation has wrong count after NOR/ISL insertion | mitigate | Verify count is 31 (29 original + NOR + ISL). Cross-reference alphabetical ordering for NOR and ISL insertion points. |
| T-09-06 | D | Accidental removal of non-EU-member entities during leftover cleanup (e.g., removing Iceland-but-Norway instead of actual leftover entries) | mitigate | Explicitly list each entity to remove. Verify each by name. Cross-check against European Federation country_codes to ensure accidentally removed countries aren't needed. |
</threat_model>

<verification>
1. Verify no (wip) tags in Northern Europe (entity-config.json + borders.kml)
2. Verify entity-config.json: European Federation has NOR and ISL in country_codes (31 total); Iceland, Norway, Ireland removed; leftover EU member entities removed (Germany, Spain, Portugal, Italy, Austria, Greece, Croatia, Cyprus, Malta); UK entry updated with late-revolutionary classification; Åland Islands added
3. Verify entity-config.json folder_hierarchy: Northern Europe → [United Kingdom]
4. Verify borders.kml: Northern Europe folder renamed (no wip), contains only United Kingdom folder
5. Verify borders.kml: Iceland and Norway Placemarks moved to European Federation folder with updated description anchors
6. Verify borders.kml: Åland Islands Placemark exists
7. Verify borders.kml is well-formed XML
8. Verify entity-config.json is valid JSON (python3 -m json.tool passes)
</verification>

<success_criteria>
- entity-config.json: European Federation (NOR+ISL added, 31 country_codes), removed entities confirmed absent, UK entry updated, Åland added, Northern Europe folder_hierarchy = [United Kingdom]
- borders.kml: Northern Europe folder renamed (no wip), UK folder retained, Iceland/Norway polygons merged into European Federation, Åland Placemark present
- KML structure is valid — folder/entity consistency between config and KML
- All changes scoped to Northern Europe region only — no other region affected
</success_criteria>

<output>
After completion, create `.planning/phases/09-northern-europe-review/09-01-SUMMARY.md`
</output>
