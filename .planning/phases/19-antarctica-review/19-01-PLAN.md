---
phase: 19-antarctica-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/user_colors.json
autonomous: true
requirements:
  - ANTA-01
user_setup: []
must_haves:
  truths:
    - "borders.kml Antarctica (wip) folder renamed to Antarctica"
    - "borders.kml contains 7 claim-zone subfolders replacing the single Antarctica polygon subfolder"
    - "entity-config.json folder_hierarchy has 'Antarctica' (not 'Antarctica (wip)') with 7 claim-zone entity IDs"
    - "entity-config.json has 7 distinct entity entries for each claim zone (replacing the single 'Antarctica' entity)"
    - "user_colors.json has 7 color entries for the claim zones (existing Antarctica color repurposed for AAT, 6 new entries)"
    - "entity-config.json remains valid JSON"
    - "borders.kml remains valid XML/KML"
  artifacts:
    - path: "2050-snapshot/kml/borders.kml"
      provides: "7 claim-zone KML subfolders with ice-shelf-boundary polygons"
      contains: "<name>Australian Antarctic Territory</name>"
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "7 claim-zone entity entries + renamed folder hierarchy"
      contains: "\"Antarctica\":"
    - path: "2050-snapshot/kml/user_colors.json"
      provides: "KML style definitions for all 7 claim zones"
      contains: "Ross Dependency"
  key_links:
    - from: "entity-config.json folder_hierarchy"
      to: "borders.kml folder names"
      via: "folder group key must match KML folder <name>"
      pattern: "Antarctica"
    - from: "entity-config.json entity entries"
      to: "user_colors.json style entries"
      via: "entity name is the style lookup key"
      pattern: "entity name === user_colors key"
---

<objective>
Replace the single Antarctica coastline-based polygon in borders.kml with 7 claim-zone subfolders using ice-shelf-boundary geometry, update entity-config.json with 7 distinct entity entries, add 6 new color styles to user_colors.json, and rename the (wip) folder. Per D-01 (claim zones as distinct KML entities), D-03 (ice shelf boundaries), and D-04 ((wip) removal).

Purpose: The current single-polygon Antarctica representation does not reflect the 2050 governance reality of 7 distinct claim zones under the ATS partial opening regime. KML viewers need to see per-claimant zones.

Output: borders.kml with 7 claim-zone subfolders (ice-shelf-boundary polygons), entity-config.json with 7 entity entries, user_colors.json with 7 styles.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-antarctica-review/19-CONTEXT.md
@.planning/phases/19-antarctica-review/19-RESEARCH.md
@.planning/phases/18-polynesia-review/18-01-SUMMARY.md

<interfaces>
<!-- Current Antarctica KML structure to be replaced (borders.kml lines 41907-~42400) -->
Current borders.kml structure:
```xml
<Folder>
  <name>Antarctica (wip)</name>
  <Folder>
    <name>Antarctica</name>
    <Placemark><name>Antarctica</name>...</Placemark>  <!-- ~27 polygon patches, coastline-based -->
    <Placemark><name>Antarctica</name>...</Placemark>
    ...
  </Folder>
</Folder>
```

<!-- Current entity-config.json Antarctica folder_hierarchy (line 203) -->
```json
"Antarctica (wip)": ["Antarctica"]
```

<!-- Current Antarctica entity entry (lines 1715-1723) -->
```json
"Antarctica": {
  "type": "entity",
  "category": "global",
  "source": "country",
  "country_code": "ATA",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
}
```

<!-- Current Antarctica user_colors.json entry -->
```json
"Antarctica": {"hex": "#1f2d4a", "kml_fill": "804a2d1f", "kml_line": "ff4a2d1f", "width": 1.0}
```

<!-- Target: 7 claim zones from RESEARCH.md Section 2 -->
Claim zones to create:
| # | Entity ID | KML Folder Name | Sector | Style |
|---|-----------|----------------|--------|-------|
| 1 | Australian Antarctic Territory | Australian Antarctic Territory | 45°E–136°E, 142°E–160°E | #1f2d4a (existing, repurposed) |
| 2 | Ross Dependency | Ross Dependency | 160°E–150°W | #1a6b4d (NZ-associated dark teal) |
| 3 | Adélie Land | Adélie Land | 136°E–142°E | #2b5c8f (EU/France blue) |
| 4 | Dronning Maud Land | Dronning Maud Land | 20°W–45°E | #3a6b8f (EU/Norway blue) |
| 5 | Argentine-Chilean Peninsula | Argentine-Chilean Peninsula | 53°W–90°W | #8b3a2b (South American warm red) |
| 6 | British Antarctic Territory | British Antarctic Territory | 20°W–53°W | #8a8a8a (dormant grey, 50% opacity) |
| 7 | Chinese Marie Byrd Land | Chinese Marie Byrd Land | 90°W–150°W | #bf2c23 (China-associated red) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download SCAR ADD ice-shelf coastline data and generate 7 claim-zone KML polygons</name>
  <files>2050-snapshot/kml/borders.kml</files>
  <read_first>
    - .planning/phases/19-antarctica-review/19-RESEARCH.md (Section 1: Ice Shelf Boundary Datasets, Section 4: KML Polygon Strategy)
    - .planning/phases/19-antarctica-review/19-CONTEXT.md (D-01 through D-04)
    - 2050-snapshot/kml/borders.kml (lines 41907-42400 — the entire Antarctica (wip) folder section to replace)
    - .planning/phases/18-polynesia-review/18-01-SUMMARY.md (for KML folder structure pattern from prior phase)
  </read_first>
  <acceptance_criteria>
    - `grep -c '<name>Antarctica (wip)</name>' 2050-snapshot/kml/borders.kml` returns 0 (renamed)
    - `grep -c '<name>Antarctica</name>' 2050-snapshot/kml/borders.kml` returns 1 (only the top-level folder, no Placemarks use this name)
    - `grep -c '<name>Australian Antarctic Territory</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>Ross Dependency</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>Adélie Land</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>Dronning Maud Land</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>Argentine-Chilean Peninsula</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>British Antarctic Territory</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - `grep -c '<name>Chinese Marie Byrd Land</name>' 2050-snapshot/kml/borders.kml` returns >= 1
    - Each claim-zone Folder contains `<altitudeMode>clampToGround</altitudeMode>` on all Polygons
    - Each claim-zone Folder has a `<description>` with `See: 2050-snapshot/domains/borders-geopolitics.md#antarctica`
    - `xmllint --noout 2050-snapshot/kml/borders.kml` exits 0 (valid XML, if xmllint available; otherwise python-based XML parse check)
  </acceptance_criteria>
  <action>
**Step A: Obtain ice-shelf coastline geometry.**
Attempt to download the SCAR ADD medium-resolution coastline GeoPackage from https://www.add.scar.org/. If direct download is not feasible (requires interactive browser), use the existing GADM Antarctica coastline polygons from the current borders.kml as the source geometry, with a note that ice-shelf-accurate boundaries can be refined later when ADD data is available. The claim-zone sector subdivision is the primary structural change — the geometry source is secondary.

**Step B: Subdivide the continent into 7 claim zones by longitudinal sector.**

Use a Python script (write to a temp file, execute, then discard) to:
1. Parse existing Antarctica polygon coordinates from the current borders.kml (lines 41907-42400)
2. For each coordinate point in the existing polygons, determine which claim zone it belongs to based on its longitude
3. Generate one KML `<Folder>` per claim zone containing the relevant polygon patches
4. Each claim-zone `<Placemark>` uses:
   - `<name>` = the claim zone name (e.g., "Australian Antarctic Territory")
   - `<styleUrl>` = a unique style ID (e.g., `#__antarctica_aat_style`)
   - `<description>` = `See: 2050-snapshot/domains/borders-geopolitics.md#antarctica`
   - `<Polygon><altitudeMode>clampToGround</altitudeMode>` wrapping the coordinates
5. For the Australian Antarctic Territory (split by Adélie Land wedge at 136°E–142°E): coordinates with longitude 45°E–136°E AND 142°E–160°E are both assigned to AAT

**Sector assignment logic:**
- 20°W to 45°E → Dronning Maud Land
- 45°E to 136°E → Australian Antarctic Territory
- 136°E to 142°E → Adélie Land
- 142°E to 160°E → Australian Antarctic Territory (continued)
- 160°E to 150°W → Ross Dependency
- 150°W to 90°W → Chinese Marie Byrd Land
- 90°W to 53°W → Argentine-Chilean Peninsula
- 53°W to 20°W → British Antarctic Territory

**Step C: Replace the existing Antarctica KML in borders.kml.**
1. Locate the `<Folder><name>Antarctica (wip)</name>` opening tag (line 41907)
2. Find the corresponding closing `</Folder>` tags (lines 41904-41906 — the 3 closing tags after the last Placemark)
3. Replace the entire Antarctica (wip) folder block with a new top-level `<Folder><name>Antarctica</name>` containing 7 sub-folders (one per claim zone), each with their polygon Placemarks
4. Ensure proper XML indentation matching the surrounding KML (use 2-space indent, consistent with existing)

**Step D: Add 7 KML style definitions.**
At the top of borders.kml (in the `<Document>` styles section, where other `#__managed_style_*` entries live), add 7 new `<Style>` elements, one per claim zone. Each style needs an `<IconStyle>`, `<LineStyle>`, and `<PolyStyle>` with the hex colors from RESEARCH.md. Use the existing managed style pattern — find an existing `#__managed_style_*` entry and replicate its structure with the new colors.

Style hex values:
1. AAT: #1f2d4a (keep existing Antarctica style)
2. Ross Dependency: #1a6b4d
3. Adélie Land: #2b5c8f
4. Dronning Maud Land: #3a6b8f
5. Argentine-Chilean Peninsula: #8b3a2b
6. British Antarctic Territory: #8a8a8a (with 50% fill opacity for dormant status)
7. Chinese Marie Byrd Land: #bf2c23

If the KML uses a managed style system (style IDs like `#__managed_style_*`), follow that pattern. Add 6 new managed style IDs after the last existing one, then reference them in the claim-zone Placemarks. The existing Antarctica style (`#__managed_style_000000000000018F`) is repurposed for the Australian Antarctic Territory — update its color to #1f2d4a if different, or keep if already matching.

**Note on ice-shelf geometry:** The existing coastline polygons are acceptable as a v1 geometry. The structural change (7 claim zones replacing single continent) is the plan's primary deliverable. If SCAR ADD data cannot be downloaded programmatically, use existing GADM geometry and document that ice-shelf refinement is a follow-up task. The polygon subdivision by longitude sector is the key implementation — it makes Antarctica's 2050 governance visible in Google Earth.

**Note on KML folder depth:** The target structure is:
```
<Folder><name>Antarctica</name>
  <Folder><name>Australian Antarctic Territory</name>
    <Placemark>...polygon patches...</Placemark>
  </Folder>
  <Folder><name>Ross Dependency</name>...</Folder>
  <Folder><name>Adélie Land</name>...</Folder>
  <Folder><name>Dronning Maud Land</name>...</Folder>
  <Folder><name>Argentine-Chilean Peninsula</name>...</Folder>
  <Folder><name>British Antarctic Territory</name>...</Folder>
  <Folder><name>Chinese Marie Byrd Land</name>...</Folder>
</Folder>
```
  </action>
  <verify>
    <automated>python3 -c "
import json, xml.etree.ElementTree as ET
# Verify entity-config.json
with open('2050-snapshot/kml/entity-config.json') as f:
    config = json.load(f)
fh = config['folder_hierarchy']
assert 'Antarctica' in fh, 'Missing Antarctica folder'
assert 'Antarctica (wip)' not in fh, 'Still has (wip) tag'
entities = config['entities']
required = ['Australian Antarctic Territory','Ross Dependency','Adélie Land','Dronning Maud Land','Argentine-Chilean Peninsula','British Antarctic Territory','Chinese Marie Byrd Land']
for e in required:
    assert e in entities, f'Missing entity: {e}'
# Verify user_colors.json
with open('2050-snapshot/kml/user_colors.json') as f:
    colors = json.load(f)
for e in required:
    assert e in colors, f'Missing color: {e}'
print('PASS: entity-config.json and user_colors.json valid')
" && echo "PASS: all checks"</automated>
  </verify>
  <done>
borders.kml has 7 claim-zone subfolders under a renamed Antarctica folder. entity-config.json has 7 entity entries and renamed folder_hierarchy. user_colors.json has 7 color entries. KML is valid XML. No (wip) tags remain.
</done>
</task>

<task type="auto">
  <name>Task 2: Update entity-config.json with 7 claim-zone entities and rename folder hierarchy</name>
  <files>2050-snapshot/kml/entity-config.json, 2050-snapshot/kml/user_colors.json</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (lines 203-205 for folder_hierarchy, lines 1715-1723 for existing Antarctica entity)
    - 2050-snapshot/kml/user_colors.json (existing Antarctica entry)
    - .planning/phases/19-antarctica-review/19-RESEARCH.md (Section 4: entity-config.json Updates)
  </read_first>
  <acceptance_criteria>
    - `python3 -c "import json; c=json.load(open('2050-snapshot/kml/entity-config.json')); assert 'Antarctica' in c['folder_hierarchy']; assert 'Antarctica (wip)' not in c['folder_hierarchy']"` exits 0
    - `python3 -c "import json; c=json.load(open('2050-snapshot/kml/entity-config.json')); a=c['folder_hierarchy']['Antarctica']; assert len(a)==7; assert 'Australian Antarctic Territory' in a"` exits 0
    - `python3 -c "import json; c=json.load(open('2050-snapshot/kml/entity-config.json')); assert 'Australian Antarctic Territory' in c['entities']; e=c['entities']['Australian Antarctic Territory']; assert e['type']=='entity'; assert e['country_code']=='ATA-AUS'"` exits 0
    - `python3 -c "import json; c=json.load(open('2050-snapshot/kml/user_colors.json')); assert 'Australian Antarctic Territory' in c; assert 'Chinese Marie Byrd Land' in c"` exits 0
    - `jsonlint 2050-snapshot/kml/entity-config.json` exits 0 (valid JSON, or python json.load check)
  </acceptance_criteria>
  <action>
**Part A: entity-config.json — folder_hierarchy rename.**
1. In `folder_hierarchy`, rename key `"Antarctica (wip)"` → `"Antarctica"`
2. Replace the single-element array `["Antarctica"]` with the 7 claim-zone entity IDs:
```json
"Antarctica": [
  "Australian Antarctic Territory",
  "Ross Dependency",
  "Adélie Land",
  "Dronning Maud Land",
  "Argentine-Chilean Peninsula",
  "British Antarctic Territory",
  "Chinese Marie Byrd Land"
]
```

**Part B: entity-config.json — replace single entity with 7 claim-zone entities.**
1. Remove the existing `"Antarctica"` entity entry (lines 1715-1723)
2. Add 7 new entity entries, each following this template:

```json
"Australian Antarctic Territory": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-AUS",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"Ross Dependency": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-NZ",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"Adélie Land": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-FR",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"Dronning Maud Land": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-NO",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"Argentine-Chilean Peninsula": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-ARCL",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"British Antarctic Territory": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-GB",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
},
"Chinese Marie Byrd Land": {
  "type": "entity",
  "category": "global",
  "source": "custom",
  "country_code": "ATA-CN",
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "antarctica",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#antarctica"
}
```

Insert these 7 entries where the old `"Antarctica"` entity was (between lines 1714 and 1724), maintaining alphabetical order among the surrounding entities.

**Part C: user_colors.json — add 6 new color entries.**
The existing `"Antarctica"` color entry is repurposed for Australian Antarctic Territory:
1. Rename key `"Antarctica"` → `"Australian Antarctic Territory"` (keep hex #1f2d4a)
2. Add 6 new entries:

```json
"Ross Dependency": {"hex": "#1a6b4d", "kml_fill": "804d6b1a", "kml_line": "ff4d6b1a", "width": 1.0},
"Adélie Land": {"hex": "#2b5c8f", "kml_fill": "808f5c2b", "kml_line": "ff8f5c2b", "width": 1.0},
"Dronning Maud Land": {"hex": "#3a6b8f", "kml_fill": "808f6b3a", "kml_line": "ff8f6b3a", "width": 1.0},
"Argentine-Chilean Peninsula": {"hex": "#8b3a2b", "kml_fill": "802b3a8b", "kml_line": "ff2b3a8b", "width": 1.0},
"British Antarctic Territory": {"hex": "#8a8a8a", "kml_fill": "808a8a8a", "kml_line": "ff8a8a8a", "width": 1.0},
"Chinese Marie Byrd Land": {"hex": "#bf2c23", "kml_fill": "80232cbf", "kml_line": "ff232cbf", "width": 1.0}
```

Note on kml_fill/kml_line: These are AABBGGRR hex format (alpha + blue + green + red, reversed from normal RRGGBBAA). Compute correctly: kml_fill = "80" + hex[5:7] + hex[3:5] + hex[1:3] (50% alpha). kml_line = "ff" + hex[5:7] + hex[3:5] + hex[1:3] (100% alpha). kml_fill for British Antarctic Territory uses "40" alpha (25% opacity) to signal dormant status.

**Exact kml_fill/kml_line values (verified):**
- Ross Dependency #1a6b4d: fill=804d6b1a, line=ff4d6b1a
- Adélie Land #2b5c8f: fill=808f5c2b, line=ff8f5c2b
- Dronning Maud Land #3a6b8f: fill=808f6b3a, line=ff8f6b3a
- Argentine-Chilean Peninsula #8b3a2b: fill=802b3a8b, line=ff2b3a8b
- British Antarctic Territory #8a8a8a: fill=408a8a8a, line=ff8a8a8a (25% fill alpha for dormant)
- Chinese Marie Byrd Land #bf2c23: fill=80232cbf, line=ff232cbf

Insert these entries into user_colors.json, maintaining alphabetical order among the existing color entries.
  </action>
  <verify>
    <automated>python3 -c "
import json
# Full validation
with open('2050-snapshot/kml/entity-config.json') as f:
    config = json.load(f)
assert 'Antarctica' in config['folder_hierarchy']
assert len(config['folder_hierarchy']['Antarctica']) == 7
required = ['Australian Antarctic Territory','Ross Dependency','Adélie Land','Dronning Maud Land','Argentine-Chilean Peninsula','British Antarctic Territory','Chinese Marie Byrd Land']
for e in required:
    assert e in config['entities'], f'Missing entity: {e}'
    ent = config['entities'][e]
    assert ent['type'] == 'entity'
    assert 'ATA-' in ent.get('country_code', ''), f'Bad country_code for {e}'
with open('2050-snapshot/kml/user_colors.json') as f:
    colors = json.load(f)
for e in required:
    assert e in colors, f'Missing color: {e}'
    c = colors[e]
    assert 'hex' in c and 'kml_fill' in c and 'kml_line' in c, f'Bad color entry for {e}'
print('PASS: All entity-config and user_colors valid')
"</automated>
  </verify>
  <done>
entity-config.json has 7 claim-zone entity entries (country_codes ATA-AUS through ATA-CN) and folder_hierarchy renamed to Antarctica with 7 entries. user_colors.json has 7 color entries with correct AABBGGRR kml values. Both files remain valid JSON.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No user input or network boundaries — this plan modifies local KML and JSON files only.

| Boundary | Description |
|----------|-------------|
| None | All operations are local file edits |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | entity-config.json / user_colors.json | accept | Local file edits only — no external input. JSON validity verified in acceptance criteria. |
</threat_model>

<verification>
1. `xmllint --noout 2050-snapshot/kml/borders.kml` — KML is valid XML
2. `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json')); print('OK')"` — entity-config is valid JSON
3. `python3 -c "import json; json.load(open('2050-snapshot/kml/user_colors.json')); print('OK')"` — user_colors is valid JSON
4. Open borders.kml in Google Earth Pro — verify 7 claim-zone subfolders visible under Antarctica, each with colored polygons and correct sector boundaries
</verification>

<success_criteria>
- [x] Antarctica (wip) folder renamed to Antarctica in both borders.kml and entity-config.json
- [x] 7 claim-zone subfolders exist in borders.kml with distinct KML styles
- [x] 7 entity entries in entity-config.json with country_codes ATA-{claimant}
- [x] 7 color entries in user_colors.json with correct kml_fill/kml_line
- [x] KML is valid XML, JSON files are valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/19-antarctica-review/19-01-SUMMARY.md`
</output>
