---
phase: 14-western-europe-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/borders.kml
autonomous: true
requirements:
  - EURA-09
user_setup: []
must_haves:
  truths:
    - "Switzerland standalone entity entry removed from entity-config.json (lines 1276-1284)"
    - "CHE added to European Federation country_codes list in entity-config.json (line ~708)"
    - "LIE added to European Federation country_codes for absorbed Liechtenstein"
    - "Switzerland polygon merged into European Federation multi-polygon in borders.kml"
    - "European Federation KML polygon renders correctly with Swiss territory included"
    - "All 6 Western EU member ISO codes (AUT, BEL, FRA, DEU, LUX, NLD) confirmed present in EU Federation country_codes"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Switzerland standalone entry removed; CHE+LIE added to EU Federation country_codes"
      contains: "\"CHE\""
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Switzerland polygon merged into European Federation multi-polygon"
      contains: "<name>European Federation</name>"
  key_links:
    - from: "entity-config.json"
      to: "borders.kml"
      via: "entity-config.json source:group directive drives KML polygon generation"
      pattern: "country_codes.*CHE"
---

<objective>
Merge Switzerland and Liechtenstein into the European Federation KML entity.

**Purpose:** Implement locked decisions D-01 (Switzerland = full EU member), D-02 (CHE removed from standalone entity entry), D-03 (Switzerland KML merged into EU Federation), D-04 (section_anchor removed), and D-05 (Liechtenstein absorbed). These are the KML/entity-config structural changes needed before domain doc sub-entries can be written.

**Output:** Updated entity-config.json and borders.kml with Switzerland and Liechtenstein absorbed into the European Federation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-western-europe-review/14-CONTEXT.md
@2050-snapshot/kml/entity-config.json
@2050-snapshot/kml/borders.kml

<read_first>
Before modifying anything, read:
- `2050-snapshot/kml/entity-config.json` lines 1276-1284 (Switzerland standalone entry to remove)
- `2050-snapshot/kml/entity-config.json` lines 662-735 (European Federation entry, especially country_codes section and add_manual_paths)
- `2050-snapshot/kml/borders.kml` — search for `<name>Switzerland</name>` or `<name>CHE</name>` to find the polygon to remove/merge
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entity Config — Remove Switzerland standalone, add CHE+LIE to EU Federation</name>
  <files>2050-snapshot/kml/entity-config.json</files>
  <read_first>
    2050-snapshot/kml/entity-config.json lines 1276-1284 (the Switzerland entry to delete)
    2050-snapshot/kml/entity-config.json lines 662-735 (the European Federation entry to modify)
  </read_first>
  <action>
    Modify entity-config.json per D-02, D-04, D-05:

    1. **Remove the Switzerland standalone entry** (lines 1276-1284):
       ```json
       "Switzerland": {
         "type": "entity",
         "category": "global",
         "source": "country",
         "country_code": "CHE",
         "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
         "section_anchor": "",
         "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md"
       },
       ```
       Delete this entire block. Ensure the preceding Peru entry's trailing comma is preserved or the following Ukraine entry is not orphaned.

    2. **Add CHE (Switzerland) to the European Federation `country_codes` array.** Insert `"CHE"` in alphabetical order among the existing codes (around line 708, after `"BIH"` and before `"DNK"` would be before `"AND"` — or just before `"AND"` to maintain rough alphabetical). Also add `"LIE"` for Liechtenstein (absorbed with Switzerland per D-05).

    3. **If Liechtenstein has no entry anywhere**, confirm it was never in entity-config.json. If it was, remove its standalone entry too. Per D-05, Liechtenstein follows Switzerland's outcome — absorbed into EU Federation, not a separate entity.

    4. **Verify** that AUT, BEL, FRA, DEU, LUX, NLD are all present in the European Federation country_codes (they should be from lines 682-687). If any are missing, add them.

    5. **Verify** the Switzerland standalone entry removal did not create JSON syntax errors (trailing commas, missing commas).
  </action>
  <verify>
    <automated>node -e "const c=require('./2050-snapshot/kml/entity-config.json'); const eu=c['European Federation']; console.log('CHE in EU:', eu.country_codes.includes('CHE')); console.log('LIE in EU:', eu.country_codes.includes('LIE')); console.log('Switzerland standalone:', !!c.Switzerland);"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const c=require('./2050-snapshot/kml/entity-config.json'); console.log(!c.Switzerland)"` prints `true`
    - `node -e "const c=require('./2050-snapshot/kml/entity-config.json'); console.log(c['European Federation'].country_codes.includes('CHE'))"` prints `true`
    - `node -e "const c=require('./2050-snapshot/kml/entity-config.json'); console.log(c['European Federation'].country_codes.includes('LIE'))"` prints `true`
    - JSON parse succeeds without syntax errors
  </acceptance_criteria>
  <done>Switzerland standalone entry removed from entity-config.json. CHE and LIE added to EU Federation country_codes. JSON parseable.</done>
</task>

<task type="auto">
  <name>Task 2: KML — Merge Switzerland polygon into European Federation</name>
  <files>2050-snapshot/kml/borders.kml</files>
  <read_first>
    2050-snapshot/kml/borders.kml — search for `<name>Switzerland</name>` or `<name>CHE</name>` to locate the Switzerland polygon/Placemark
    2050-snapshot/kml/borders.kml — search for `<name>European Federation</name>` or `<name>European Union</name>` to locate the EU multi-polygon
  </read_first>
  <action>
    Merge Switzerland's KML polygon into the European Federation multi-polygon per D-03:

    1. **Find Switzerland Placemark** in borders.kml. Search by `<name>Switzerland</name>`, `<name>CHE</name>`, or `<name>Swiss Confederation</name>`. The Switzerland entry will be a `<Placemark>` containing `<Polygon>` or `<MultiGeometry>` with coordinate data.

    2. **Find the European Federation Placemark.** Search by `<name>European Federation</name>`. It should be a `<Placemark>` with `<MultiGeometry>` containing multiple `<Polygon>` elements.

    3. **Extract Switzerland's geometry** — the `<Polygon>` or `<MultiGeometry>` XML from Switzerland's Placemark.

    4. **Merge into European Federation:** Insert Switzerland's polygon(s) as additional `<Polygon>` elements inside the EU Federation's `<MultiGeometry>`. Place them after the existing polygons.

    5. **Remove Switzerland Placemark.** Delete the entire Switzerland Placemark block. Verify no empty folders or broken XML result.

    6. **Check for Liechtenstein** — if there's a separate Liechtenstein Placemark, merge it into EU Federation the same way (absorbed with Switzerland per D-05). Remove the standalone Placemark.

    7. **Maintain XML validity** — ensure no unclosed tags, no duplicate IDs, no broken hierarchy.
  </action>
  <verify>
    <automated>node -e "
const fs=require('fs');
const xml=fs.readFileSync('./2050-snapshot/kml/borders.kml','utf8');
// Check Switzerland Placemark removed
const swiss=/<name>Switzerland<\/name>/.test(xml);
const che=/<name>CHE<\/name>/.test(xml);
// Check EU Federation still present (should have absorbed Switzerland)
const eu=/<name>European Federation<\/name>/.test(xml);
console.log('Switz Placemark removed:', !swiss && !che);
console.log('EU Federation present:', eu);
"