---
phase: 08-eastern-europe-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/entity-config.json
autonomous: true
requirements:
  - EURA-03
user_setup: []
must_haves:
  truths:
    - "Eurasia and Eastern Europe KML folders no longer have (wip) tags"
    - "European Union entity exists in entity-config.json as a group with all EU member country_codes"
    - "Individual EU member entries (Poland, Czechia, Slovakia, Bulgaria, Romania, Hungary, Moldova) removed from entity-config.json"
    - "Russia entity config updated with expanded territorial description; Belarus and Ukraine have section_anchor and see_path"
    - "Eastern Europe folder_hierarchy lists European Union, Russia, Belarus, Ukraine"
    - "borders.kml Eastern Europe folder contains European Union (merged EU member polygons), Russia, Belarus, Ukraine — no individual EU member folders"
    - "Moldova folder and Placemarks removed from borders.kml"
    - "Russia entity descriptions note Union State context and expanded territory (Crimea + 4 oblasts)"
    - "Ukraine entity descriptions note reduced territory + Transnistria"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Updated entity config with European Union entity, removed individual EU members, updated Russia/Belarus/Ukraine"
      contains: "European Union group entity with 27 country_codes; no Poland, Czechia, Slovakia, Bulgaria, Romania, Hungary, Moldova entities; Eastern Europe folder_hierarchy: [European Union, Russia, Belarus, Ukraine]"
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Updated borders.kml with Eastern Europe restructuring: EU member polygons merged under European Union folder, Moldova removed, descriptions updated"
      contains: "European Union folder with merged member polygons; Russia folder + Placemarks with Union State descriptions; Belarus folder + Placemark with anchor; Ukraine folder with reduced-territory description; no Moldova folder"
  key_links:
    - from: "entity-config.json::folder_hierarchy Eastern Europe"
      to: "entity-config.json::entities European Union"
      pattern: "folder_hierarchy references entity name European Union exactly"
    - from: "borders.kml::European Union Placemark descriptions"
      to: "borders-geopolitics.md#european-union"
      pattern: "See: 2050-snapshot/domains/borders-geopolitics.md#european-union"
    - from: "entity-config.json::entities Russia.see_path"
      to: "borders-geopolitics.md#russia"
      pattern: "See: 2050-snapshot/domains/borders-geopolitics.md#russia"
---

<objective>
Update entity-config.json and borders.kml for Eastern Europe: remove (wip) tags, restructure the Eastern Europe KML folder for the 2050 geopolitical reality (EU federalization = single entity, Union State = confederation of Russia/Belarus/Ukraine), merge EU member polygons into a single European Union entity, remove Moldova, and update all descriptions. Per D-20 through D-28.

Purpose: Eastern Europe's KML representation must reflect the 2050 reality: a federal EU absorbing all former member states, a Union State confederation of Russia/Belarus/Ukraine with separate polygons, Moldova absorbed into Romania (part of EU), Transnistria absorbed into Ukraine. The (wip) tag removal signals Eastern Europe as reviewed.
Output: Updated `entity-config.json` and `borders.kml` with corrected Eastern Europe representation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-eastern-europe-review/08-CONTEXT.md
@.planning/phases/07-eastern-asia-review/07-01-PLAN.md

<interfaces>
**Current entity-config.json — Eurasia folder_hierarchy (Eastern Europe entry):**
```json
"Eastern Europe (wip)": [
  "Belarus", "Bulgaria", "Czechia", "Hungary", "Moldova",
  "Poland", "Romania", "Russia", "Slovakia", "Ukraine"
],
```

**Current entity-config.json — Eastern Europe entity entries:**
- Russia: {source: "country", country_code: "RUS", section_anchor: "russia", see_path: "...#russia"}
- Ukraine: {source: "country", country_code: "UKR", section_anchor: "", see_path: "..."}
- Belarus: {source: "country", country_code: "BLR", section_anchor: "", see_path: "..."}
- Poland: {source: "country", country_code: "POL", section_anchor: "", see_path: "..."}
- Czechia: {source: "country", country_code: "CZE", ...}
- Slovakia: {source: "country", country_code: "SVK", ...}
- Bulgaria: {source: "country", country_code: "BGR", ...}
- Romania: {source: "country", country_code: "ROU", ...}
- Hungary: {source: "country", country_code: "HUN", ...}
- Moldova: {source: "country", country_code: "MDA", ...}

**Current borders.kml — Eastern Europe folder (starts at line ~49328):**
```xml
<Folder>
  <name>Eastern Europe (wip)</name>
  <Folder><name>Belarus</name>...</Folder>
  <Folder><name>Bulgaria</name>...</Folder>
  <Folder><name>Czechia</name>...</Folder>
  <Folder><name>Hungary</name>...</Folder>
  <Folder><name>Moldova</name>...</Folder>
  <Folder><name>Poland</name>...</Folder>
  <Folder><name>Romania</name>...</Folder>
  <Folder><name>Russia</name>... (many Placemarks)</Folder>
  <Folder><name>Slovakia</name>...</Folder>
  <Folder><name>Ukraine</name>...</Folder>
</Folder>
```

Russia has ~20+ Placemarks (multi-polygon). All EU member countries have 1-2 Placemarks each. All Placemark descriptions currently say `See: 2050-snapshot/domains/borders-geopolitics.md` (no anchor).

**EU member countries by ISO code** (27 total):
POL, CZE, SVK, HUN, ROU, BGR (Eastern — Phase 8) | DNK, EST, FIN, IRL, LVA, LTU, SWE (Northern) | AUT, BEL, FRA, DEU, LUX, NLD (Western) | HRV, CYP, GRC, ITA, MLT, PRT, SVN, ESP (Southern)

**EU members NOT in EU (non-members):** Iceland, Norway, UK, Switzerland, Albania, Bosnia, Croatia (actually IS EU), Kosovo, Montenegro, North Macedonia, Serbia, Turkey
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update entity-config.json — add European Union, remove EU members, update Russia/Belarus/Ukraine, fix folder_hierarchy</name>
  <files>2050-snapshot/kml/entity-config.json</files>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (folder_hierarchy Eurasia section, all entity entries for Eastern Europe countries, Russia entry)
  </read_first>
  <action>
    Edit `2050-snapshot/kml/entity-config.json`:

    **A. Remove (wip) from Eurasia subregion keys in folder_hierarchy:**
    - `"Eastern Europe (wip)"` → `"Eastern Europe"` 
    - Also check `"Northern Europe (wip)"`, `"Western Europe (wip)"`, `"Southern Europe (wip)"` — do NOT remove these (other phases handle them). Only remove from Eastern Europe.

    **B. Update Eastern Europe folder_hierarchy entry** to the new 4-entity structure:
    ```json
    "Eastern Europe": [
      "European Union",
      "Russia",
      "Belarus",
      "Ukraine"
    ],
    ```

    **C. Add European Union entity** — insert before the Russia entry in the entities section:
    ```json
    "European Union": {
      "type": "entity",
      "category": "global",
      "source": "group",
      "country_codes": ["POL", "CZE", "SVK", "HUN", "ROU", "BGR", "DNK", "EST", "FIN", "IRL", "LVA", "LTU", "SWE", "AUT", "BEL", "FRA", "DEU", "LUX", "NLD", "HRV", "CYP", "GRC", "ITA", "MLT", "PRT", "SVN", "ESP"],
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "european-union",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#european-union"
    },
    ```
    Per D-02, D-03, D-21. All 27 EU member ISO 3166-1 alpha-3 codes. `source: "group"` (multi-polygon entity). The section_anchor `european-union` matches the heading anchor in borders-geopolitics.md (created in Plan 02).

    **D. Remove individual EU member entity entries** — Delete these entity entries entirely (including trailing commas):
    - `"Poland": { ... },`
    - `"Czechia": { ... },`
    - `"Slovakia": { ... },`
    - `"Hungary": { ... },`
    - `"Romania": { ... },`
    - `"Bulgaria": { ... },`
    - `"Moldova": { ... },`
    Per D-02, D-11, D-12, D-23.

    **E. Update Russia entity** — Modify the existing Russia entry to reference the Union State context. Change `see_path` to include the anchor:
    ```json
    "Russia": {
      "type": "entity",
      "category": "global",
      "source": "country",
      "country_code": "RUS",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "russia",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#russia"
    },
    ```
    The `see_path` already has `#russia` — no change needed to the path. Per D-04, D-22, D-28.

    **F. Update Belarus entity** — Add section_anchor and fix see_path:
    ```json
    "Belarus": {
      "type": "entity",
      "category": "global",
      "source": "country",
      "country_code": "BLR",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "belarus",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#belarus"
    },
    ```
    Per D-14, D-28. The Belarus profile is created in Plan 02.

    **G. Update Ukraine entity** — Add section_anchor and fix see_path. Note: Ukraine keeps country_code UKR (the reduced territory is described in domain docs, not in entity config):
    ```json
    "Ukraine": {
      "type": "entity",
      "category": "global",
      "source": "country",
      "country_code": "UKR",
      "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
      "section_anchor": "ukraine",
      "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#ukraine"
    },
    ```
    Per D-15, D-28. Ukraine's reduced territory (lost 5 eastern oblasts to Russia, absorbed Transnistria) is described in the domain docs. The KML polygon boundaries are noted for user adjustment in Google Earth Pro (see Task 3).

    **Important:** Maintain valid JSON. All trailing commas correct (last entry in each object has no comma). After all edits, verify with `python3 -m json.tool`.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)
# Check EU entity
assert 'European Union' in d['entities'], 'EU entity missing'
eu = d['entities']['European Union']
assert eu['source'] == 'group', f'EU source is {eu[\"source\"]}, expected group'
assert len(eu['country_codes']) == 27, f'EU has {len(eu[\"country_codes\"])} codes, expected 27'
assert 'POL' in eu['country_codes'] and 'DEU' in eu['country_codes'] and 'FRA' in eu['country_codes'], 'Missing major EU codes'
# Check removed entities
removed = ['Poland','Czechia','Slovakia','Hungary','Romania','Bulgaria','Moldova']
for r in removed:
    assert r not in d['entities'], f'{r} still in entities'
# Check retained entities
for e in ['Russia','Belarus','Ukraine']:
    assert e in d['entities'], f'{e} missing from entities'
    assert d['entities'][e]['section_anchor'] != '', f'{e} section_anchor empty'
# Check folder hierarchy
ee = d['folder_hierarchy']['Eurasia']['Eastern Europe']
assert 'European Union' in ee and 'Russia' in ee and 'Belarus' in ee and 'Ukraine' in ee, f'EE folder wrong: {ee}'
assert '(wip)' not in str(ee), 'wip tag still present'
print('PASS: all entity-config checks passed')
"</automated>
  </verify>
  <done>
    entity-config.json: European Union entity added (27 country_codes, group source), all individual EU member entries removed (Poland, Czechia, Slovakia, Bulgaria, Romania, Hungary, Moldova), Russia/Belarus/Ukraine updated with section_anchors and see_paths, Eastern Europe folder_hierarchy restructured to [European Union, Russia, Belarus, Ukraine], (wip) removed. Per D-20 through D-28.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit borders.kml — remove (wip), restructure EU members into European Union folder, remove Moldova</name>
  <files>2050-snapshot/kml/borders.kml</files>
  <read_first>
    - 2050-snapshot/kml/borders.kml (Eastern Europe folder starts at line ~49328, find the closing </Folder> for the Eastern Europe region)
    - 2050-snapshot/kml/borders.kml (Russia folder with ~24 Placemarks at lines ~49441-49700+)
    - 2050-snapshot/kml/borders.kml (Ukraine, Moldova, Belarus folders — identify their line ranges)
  </read_first>
  <action>
    Edit `2050-snapshot/kml/borders.kml` within the Eastern Europe folder (lines ~49328 through the closing `</Folder>` that ends Eastern Europe). The edits below are listed in order of execution.

    **A. Remove (wip) tag from folder name:**
    Change line ~49328:
    `<name>Eastern Europe (wip)</name>` → `<name>Eastern Europe</name>`
    Per D-20.

    **B. Create European Union folder and move EU member polygons into it:**
    After the opening `<Folder>` for Eastern Europe, insert a new `European Union` folder that will contain all EU member country polygons.

    The approach: For each EU member country in Eastern Europe (Poland, Czechia, Slovakia, Hungary, Romania, Bulgaria), extract its `<Placemark>` elements from its country folder and move them into the new European Union folder. Then delete the now-empty country folders.

    Step-by-step:
    1. Create the European Union folder structure after the Eastern Europe folder opening:
    ```xml
    <Folder>
      <name>European Union</name>
    ```
    
    2. For each EU member country, find its folder block (e.g., `<Folder><name>Poland</name>...<Placemark>...</Placemark></Folder>`) and extract only the Placemark blocks (including `<Placemark>`, `<name>` (the polygon name), `<styleUrl>`, `<Polygon>`, and closing `</Placemark>`). Move these Placemarks inside the European Union folder. For each moved Placemark:
       - Keep the `<name>` as-is (the polygon name like "Poland" or the coordinate-derived name)
       - Keep the `<styleUrl>` as-is
       - Add or update the `<description>` to: `<description>See: 2050-snapshot/domains/borders-geopolitics.md#european-union</description>`
       Per D-21, D-28.

    3. Delete the now-empty country folders for: Poland, Czechia, Slovakia, Hungary, Romania, Bulgaria.

    4. Close the European Union folder with `</Folder>`.

    **C. Remove Moldova folder entirely:**
    Delete the entire `<Folder><name>Moldova</name>...<Placemark>...</Placemark></Folder>` block. Per D-23. Moldova's territory is absorbed into Romania (part of the EU).

    **D. Update Russia Placemark descriptions:**
    All Russia Placemarks (inside the `<Folder><name>Russia</name>` block) currently have:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md</description>`
    Change ALL Russia Placemark descriptions to:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md#russia</description>`
    Per D-04, D-28. This anchors to the Russia entry in borders-geopolitics.md.

    **E. Update Belarus Placemark descriptions:**
    Inside `<Folder><name>Belarus</name>`, the Placemark description currently has no anchor:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md</description>`
    Change to:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md#belarus</description>`
    Per D-14, D-28.

    **F. Update Ukraine Placemark descriptions:**
    Inside `<Folder><name>Ukraine</name>`, add the anchor:
    `<description>See: 2050-snapshot/domains/borders-geopolitics.md#ukraine</description>`
    Per D-15, D-28.

    **G. Verify Russia polygon presence:**
    Confirm the Russia folder contains all ~24 original Placemarks — none were accidentally moved or deleted. Per D-22.

    **H. Add a `<!-- Note: ... -->` XML comment inside the Eastern Europe folder** documenting that precise Russia/Ukraine boundary editing (Crimea + 4 oblasts transfer, Transnistria absorption) requires user adjustment in Google Earth Pro per Phase 5's D-19 programmatic-generation-with-user-refinement pattern.

    **Important:** 
    - Do NOT modify Placemarks for non-Eastern-European entities (Russia, Belarus, Ukraine — only their descriptions)
    - Do NOT modify any KML outside the Eastern Europe folder
    - Verify the KML is well-formed XML after all edits (balanced Folder and Placemark tags)
    - Use targeted find-replace within the Eastern Europe folder boundaries, not global operations
  </action>
  <verify>
    <automated>echo "=== (wip) check ===" && grep -c 'Eastern Europe (wip)' 2050-snapshot/kml/borders.kml && echo "(should be 0)" && echo "=== EU folder exists ===" && grep -c '<name>European Union</name>' 2050-snapshot/kml/borders.kml && echo "(should be >=1)" && echo "=== Old country folders removed ===" && for c in Poland Czechia Slovakia Hungary Romania Bulgaria Moldova; do echo -n "$c: "; grep -c "<name>$c</name>" 2050-snapshot/kml/borders.kml; done && echo "(all should be 0)" && echo "=== Russia, Belarus, Ukraine still exist ===" && for c in Russia Belarus Ukraine; do echo -n "$c: "; grep -c "<name>$c</name>" 2050-snapshot/kml/borders.kml; done && echo "(all should be >=1)" && echo "=== Description anchors ===" && grep -c 'borders-geopolitics.md#russia' 2050-snapshot/kml/borders.kml && grep -c 'borders-geopolitics.md#belarus' 2050-snapshot/kml/borders.kml && grep -c 'borders-geopolitics.md#ukraine' 2050-snapshot/kml/borders.kml && grep -c 'borders-geopolitics.md#european-union' 2050-snapshot/kml/borders.kml</automated>
  </verify>
  <done>
    borders.kml: Eastern Europe folder has no (wip) tag. European Union folder created containing all former Eastern European EU member polygons (Poland, Czechia, Slovakia, Hungary, Romania, Bulgaria). Moldova folder removed. Russia, Belarus, Ukraine folders retained with updated description anchors (#russia, #belarus, #ukraine). EU Placemarks have #european-union description anchors. Per D-20 through D-25, D-28.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify KML well-formedness and validate entity config JSON</name>
  <files>2050-snapshot/kml/borders.kml, 2050-snapshot/kml/entity-config.json</files>
  <action>
    Final validation checks:

    **1. Validate entity-config.json** is valid JSON:
    ```bash
    python3 -m json.tool 2050-snapshot/kml/entity-config.json > /dev/null && echo "JSON valid"
    ```

    **2. Verify borders.kml** is well-formed XML. Since KML files can be very large, check structural integrity:
    ```bash
    # Count Folder opens vs closes in the Eastern Europe region to verify balance
    python3 -c "
    with open('2050-snapshot/kml/borders.kml') as f:
        content = f.read()
    # Check that key folder names exist exactly once
    for name in ['Eastern Europe', 'European Union', 'Russia', 'Belarus', 'Ukraine']:
        count = content.count(f'<name>{name}</name>')
        print(f'{name}: {count} occurrence(s)')
    # Verify no leftover individual EU member folders
    for name in ['Poland', 'Czechia', 'Slovakia', 'Hungary', 'Romania', 'Bulgaria', 'Moldova']:
        count = content.count(f'<name>{name}</name>')
        if count > 0:
            print(f'WARNING: {name} still present ({count} times)')
    print('Structural check complete')
    "
    ```

    **3. Verify entity-config.json folder hierarchy** is consistent:
    ```bash
    python3 -c "
    import json
    with open('2050-snapshot/kml/entity-config.json') as f:
        d = json.load(f)
    ee_folder = d['folder_hierarchy']['Eurasia']['Eastern Europe']
    ee_entities = set(d['entities'].keys())
    for name in ee_folder:
        assert name in ee_entities, f'{name} in folder_hierarchy but not in entities'
    print(f'Eastern Europe folder has {len(ee_folder)} entities: {ee_folder}')
    print('Folder-entity consistency OK')
    "
    ```

    **4. Check that generate-kml.py can parse the updated config** (don't run it — just verify it can load the config):
    ```bash
    python3 -c "
    import json, sys
    sys.path.insert(0, '2050-snapshot/kml')
    # Just verify the config can be loaded by the pipeline's JSON parser
    with open('2050-snapshot/kml/entity-config.json') as f:
        d = json.load(f)
    assert 'European Union' in d['entities']
    print('Pipeline config parse OK')
    "
    ```
  </action>
  <verify>
    <automated>python3 -c "
import json
# JSON validation
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)
assert 'European Union' in d['entities']
assert 'Eastern Europe' in d['folder_hierarchy']['Eurasia']
assert '(wip)' not in str(d['folder_hierarchy']['Eurasia']['Eastern Europe'])
# KML structure check
with open('2050-snapshot/kml/borders.kml') as f:
    kml = f.read()
assert '<name>European Union</name>' in kml
assert '<name>Moldova</name>' not in kml
assert '<name>Eastern Europe (wip)</name>' not in kml
print('PASS: All final validation checks passed')
"</automated>
  </verify>
  <done>entity-config.json is valid JSON. borders.kml is structurally intact. Eastern Europe folder hierarchy is consistent between entity-config and KML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KML file edit | Local file modification — no external input, no untrusted data |
| JSON file edit | Local file modification — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D | JSON parse error from entity-config.json edits | mitigate | Validate JSON with `python3 -m json.tool` after all edits. Verify all required entity entries exist and folder_hierarchy keys match entity names. |
| T-08-02 | D | Accidental deletion of Russia/Belarus/Ukraine KML polygons during EU merge operation | mitigate | Explicitly exclude Russia, Belarus, Ukraine folders from merge operations. Verify their Placemark counts match original after edits. Add verification grep for each entity name. |
| T-08-03 | D | KML XML structural damage — mismatched Folder/Placemark tags from cut-paste operations | mitigate | Verify Folder open/close balance in the Eastern Europe region. Check that EU folder properly encloses all moved Placemarks and closes before Russia folder begins. |
| T-08-04 | D | entity-config.json folder_hierarchy stale — references entities that don't exist or missing new entities | mitigate | Cross-reference every name in Eastern Europe folder_hierarchy against entities dict keys. Assert all four entities (European Union, Russia, Belarus, Ukraine) exist in both. |
| T-08-05 | I | Entity config `country_codes` array missing an EU member — incomplete EU polygon in future pipeline runs | mitigate | List the 27 EU member codes explicitly. Verify count is 27. Cross-reference against known EU-27 membership. |
</threat_model>

<verification>
1. Verify no (wip) tags in Eastern Europe (entity-config.json + borders.kml)
2. Verify entity-config.json: European Union entity exists with 27 country_codes, individual EU member entries removed, Russia/Belarus/Ukraine have section_anchors
3. Verify entity-config.json folder_hierarchy: Eastern Europe → [European Union, Russia, Belarus, Ukraine]
4. Verify borders.kml: European Union folder contains Placemarks from former EU member countries, each with #european-union description anchor
5. Verify borders.kml: Moldova folder completely removed
6. Verify borders.kml: Russia, Belarus, Ukraine folders present with updated description anchors (#russia, #belarus, #ukraine)
7. Verify borders.kml is well-formed XML (Folder/Placemark tags balanced in Eastern Europe region)
8. Verify entity-config.json is valid JSON (python3 -m json.tool passes)
9. Verify folder_hierarchy entity names match entities dict keys (consistency check)
</verification>

<success_criteria>
- entity-config.json: European Union entity (group, 27 country_codes), individual EU member entries removed, Russia/Belarus/Ukraine with section_anchors, Eastern Europe folder_hierarchy = [European Union, Russia, Belarus, Ukraine]
- borders.kml: Eastern Europe folder renamed (no wip), European Union folder containing merged EU member polygons, Moldova removed, Russia/Belarus/Ukraine descriptions updated with proper anchors
- KML structure is valid — folder/entity consistency between config and KML
- All changes scoped to Eastern Europe region only — no other region affected
</success_criteria>

<output>
After completion, create `.planning/phases/08-eastern-europe-review/08-01-SUMMARY.md`
</output>
