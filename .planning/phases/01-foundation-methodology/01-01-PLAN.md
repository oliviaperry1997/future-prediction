---
phase: 01-foundation-methodology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .obsidian/app.json
  - .obsidian/appearance.json
  - .obsidian/community-plugins.json
  - .obsidian/core-plugins.json
  - .obsidian/hotkeys.json
  - templates/base.md
  - templates/domain-doc.md
  - templates/prediction.md
  - templates/counter-scenario.md
  - index.md
  - BASEMAP_README.md
  - .gitignore
autonomous: false
requirements: [FOUND-01]
user_setup:
  - service: obsidian
    why: "Open vault, verify Dataview plugin, and test template usage"
    dashboard_config:
      - task: "Open Obsidian → 'Open folder as vault' → Select the project repo root"
      - task: "If Dataview plugin doesn't auto-enable, go to Settings → Community Plugins → Dataview → Enable"
      - task: "Create a test note from template → Ctrl/Cmd+P → 'Templates: Insert template' → select 'Domain Doc'"

must_haves:
  truths:
    - "Author can open the project root as an Obsidian vault"
    - "Author can create a new domain document from a YAML frontmatter template"
    - "Author can navigate the project from the root index.md"
    - "Dataview plugin is enabled and available in Obsidian"
    - "Git will ignore generated/binary files per .gitignore rules"
  artifacts:
    - path: ".obsidian/app.json"
      provides: "Obsidian vault configuration (vim, spellcheck, attachment folder)"
      min_lines: 3
    - path: ".obsidian/community-plugins.json"
      provides: "Dataview plugin enabled flag"
      contains: "dataview"
    - path: "templates/base.md"
      provides: "Base YAML frontmatter schema shared across all document types"
      min_lines: 12
    - path: "templates/domain-doc.md"
      provides: "Domain document template extending base with domain + milestone fields"
      min_lines: 12
    - path: "templates/prediction.md"
      provides: "Prediction entry template extending base with confidence, target_milestone, falsifiable_statement, domain, doc_ref"
      min_lines: 15
    - path: "templates/counter-scenario.md"
      provides: "Counter-scenario template extending base with alternative_thesis, divergence_points"
      min_lines: 12
    - path: "index.md"
      provides: "Master project index with links to milestones, transitions, and basemap"
      min_lines: 40
    - path: "BASEMAP_README.md"
      provides: "Documentation of the modern-day KML basemap (source, coverage, coordinate system)"
      min_lines: 20
  key_links:
    - from: "templates/prediction.md"
      to: "meta/predictions/"
      via: "Prediction files created from this template in Plan 02"
    - from: "index.md"
      to: "2050-snapshot/index.md"
      via: "Navigation link (file created in later phase)"
    - from: "index.md"
      to: "2026-2050-transition/"
      via: "Navigation link (created in Phase 2)"
---

<objective>
Initialize the Obsidian vault, create the full directory structure, and build all YAML frontmatter templates.

**Purpose**: This is the foundational layer that every subsequent phase depends on. Without the vault, templates, and directory structure, none of the project's content can be created with the intended methodology. The templates encode the structured analysis approach and ensure consistency across all milestones.

**Output**: Operational Obsidian vault with ready-to-use templates, documented basemap, and navigable project index.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-methodology/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Obsidian vault and create project directory structure</name>
  <files>
    .obsidian/app.json
    .obsidian/appearance.json
    .obsidian/community-plugins.json
    .obsidian/core-plugins.json
    .obsidian/hotkeys.json
    .gitignore
    BASEMAP_README.md
  </files>
  <read_first>
    .planning/phases/01-foundation-methodology/01-CONTEXT.md (D-01, D-02, D-03 for directory structure decisions)
    .planning/research/ARCHITECTURE.md (section 2 for reference directory layout)
  </read_first>
  <action>
    **Step 1 — Create all directories (use mkdir -p for the full tree):**

    Create the following directory tree under the project root:

    ```
    templates/
    meta/
    meta/predictions/
    sources/
    basemap/
    basemap/kml/
    2026-2050-transition/
    2050-snapshot/
    2050-snapshot/domains/
    2050-snapshot/kml/
    2075-snapshot/
    2075-snapshot/domains/
    2075-snapshot/kml/
    2100-snapshot/
    2100-snapshot/domains/
    2100-snapshot/kml/
    ```

    (Per D-02 and D-03: flat per-milestone structure, supporting dirs at root.)

    **Step 2 — Create .obsidian/ vault configuration files:**

    `.obsidian/app.json`:
    ```json
    {
      "attachmentFolderPath": "./sources",
      "promptDelete": false,
      "showLineNumber": true,
      "spellcheck": true,
      "vimMode": false,
      "alwaysUpdateLinks": true,
      "newFileLocation": "folder",
      "newFileFolderPath": "2050-snapshot/domains",
      "readableLineLength": true,
      "strictLineBreaks": false,
      "useTab": false,
      "tabSize": 2
    }
    ```

    `.obsidian/appearance.json`:
    ```json
    {
      "accentColor": "",
      "cssTheme": "",
      "baseTheme": "moonstone",
      "translucency": false
    }
    ```

    `.obsidian/community-plugins.json` — MUST include "dataview" (per D-13, D-14):
    ```json
    ["dataview"]
    ```

    `.obsidian/core-plugins.json`:
    ```json
    [
      "file-explorer",
      "global-search",
      "switcher",
      "graph",
      "backlink",
      "outgoing-link",
      "tag-pane",
      "page-preview",
      "templates",
      "note-composer",
      "command-palette",
      "editor-status",
      "outline",
      "word-count",
      "file-recovery"
    ]
    ```

    `.obsidian/hotkeys.json` — empty array (no custom hotkeys yet):
    ```json
    []
    ```

    **Step 3 — Create `.gitignore`:**
    ```
    # Generated/binary files
    *.kmz
    *.DS_Store
    .DS_Store

    # Obsidian workspace lock
    .obsidian/workspace.json
    .obsidian/workspace-mobile.json
    ```

    **Step 4 — Create `BASEMAP_README.md`:**

    Document the modern-day KML basemap with frontmatter and sections:
    ```markdown
    ---
    title: Modern-Day Basemap
    status: final
    created: 2026-05-19
    ---

    # Modern-Day Basemap

    **Status:** Pre-existing — built by the author in Google Earth Pro.

    ## Location

    - Source KML: `basemap/kml/modern-day-borders.kmz` (or `.kml`)
    - The original modern-day basemap file should be placed here.

    ## Coverage

    - Sovereign state boundaries as of 2026
    - Maritime boundaries and Exclusive Economic Zones (EEZs)
    - Major disputed territories with demarcation lines

    ## Coordinate System

    - WGS-84 (standard for Google Earth / KML)

    ## Usage

    - This is the t=0 starting point for all projections in this project
    - Each milestone's `doc.kml` can reference this via NetworkLink for visual diff
    - Current basemap covers all UN member states plus major de facto states

    ## Caveats

    - Basemap reflects the author's best understanding of current borders
    - Some disputed boundaries (Western Sahara, Kashmir, Crimea, etc.) use the de facto control line
    - The basemap is for reference only — it will be modified in milestone KML files

    ## See Also

    For basemap updates, edit the KML directly in Google Earth Pro and save back to `basemap/kml/`.
    ```
  </action>
  <verify>
    <automated>
      ls .obsidian/app.json .obsidian/community-plugins.json .obsidian/core-plugins.json .gitignore BASEMAP_README.md templates/ meta/ 2050-snapshot/domains/ 2075-snapshot/kml/ 2100-snapshot/ 2026-2050-transition/ sources/ basemap/kml/ 2>/dev/null | wc -l | grep -q "13" && echo "ALL_DIRS_AND_FILES_EXIST" || echo "MISSING_FILES"
    </automated>
  </verify>
  <acceptance_criteria>
    - 12 directories plus .obsidian/ exist (verify with ls)
    - .obsidian/community-plugins.json contains "dataview" string
    - .gitignore contains "*.kmz"
    - BASEMAP_README.md contains "WGS-84"
    - app.json contains '"showLineNumber": true'
  </acceptance_criteria>
  <done>
    Vault initialized, directory tree complete, Dataview plugin configured, .gitignore and BASEMAP_README written.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create YAML frontmatter templates and root index.md</name>
  <files>
    templates/base.md
    templates/domain-doc.md
    templates/prediction.md
    templates/counter-scenario.md
    index.md
  </files>
  <read_first>
    .planning/phases/01-foundation-methodology/01-CONTEXT.md (D-04 through D-07 for schema decisions, D-12 for counter-scenario thesis)
    .planning/research/ARCHITECTURE.md (section 3 for domain doc template structure, section 5 for cross-referencing conventions)
  </read_first>
  <action>
    Create four YAML frontmatter template files and the root index.md. Each template lives in `templates/` and is used via Obsidian's Templater or Core Templates plugin (Ctrl/Cmd+P → Templates: Insert template).

    **`templates/base.md`** — Shared base schema for all document types (per D-04):
    ```markdown
    ---
    title:
    status: draft      # draft | review | final
    created: <% tp.date.now("YYYY-MM-DD") %>
    updated: <% tp.date.now("YYYY-MM-DD") %>
    tags: []
    ---

    # {{title}}

    **Status:** `= this.status`
    **Created:** `= this.created`
    **Updated:** `= this.updated`

    ## Overview

    <!-- One-paragraph description -->

    ## Content

    <!-- Main body -->
    ```

    **`templates/domain-doc.md`** — Domain document template. Extends base schema with domain + milestone (per D-05). Uses `→ See KML:` markers per research conventions (ARCHITECTURE.md section 5.1):
    ```markdown
    ---
    title:
    status: draft
    created: <% tp.date.now("YYYY-MM-DD") %>
    updated: <% tp.date.now("YYYY-MM-DD") %>
    tags: []
    domain:               # borders | climate | technology | economy | demographics | culture
    milestone:            # 2050 | 2075 | 2100
    ---

    # {{title}}: {{milestone}} Snapshot

    **Domain:** `= this.domain`
    **Milestone:** `= this.milestone`
    **Status:** `= this.status`

    ## Key Changes From Previous Milestone

    - Change 1: <!-- Description, reference transition doc -->

    ## Analysis

    <!-- Domain content organized by region or subdomain -->

    ### Region / Entity

    → See KML: [Placemark Name in corresponding .kml file]

    ## Driving Forces

    <!-- Key drivers from the transition period -->

    ## Interactions With Other Domains

    <!-- Cross-domain coupling notes -->

    ## Key Uncertainties

    <!-- What could change this picture? -->
    ```

    **`templates/prediction.md`** — Prediction entry template. Extends base with prediction-specific fields (per D-06, D-08, D-09, D-10). Each prediction is its own `.md` file in `meta/predictions/`:
    ```markdown
    ---
    title:
    status: draft
    created: <% tp.date.now("YYYY-MM-DD") %>
    updated: <% tp.date.now("YYYY-MM-DD") %>
    tags: []
    confidence: MEDIUM    # HIGH | MEDIUM | LOW
    target_milestone:     # 2050 | 2075 | 2100
    falsifiable_statement:
    domain:               # borders | climate | technology | economy | demographics | culture
    doc_ref:              # Relative path to the domain doc this prediction belongs to
    ---

    # {{title}}

    **Confidence:** `= this.confidence` | **Target:** `= this.target_milestone`
    **Domain:** `= this.domain` | **Doc Ref:** `= this.doc_ref`

    ## Falsifiable Statement

    <!-- Specific, observable claim. Example: "By 2050, the US will have formally dissolved as a federal union with at least 3 successor states recognized by the UN." -->

    ## Reasoning

    <!-- The logic and evidence chain -->

    ## What Would Falsify This

    <!-- Specific events or data that would disprove this prediction -->

    ## Confidence Criteria

    - **HIGH:** Strong evidence across multiple domains, consistent with established trends
    - **MEDIUM:** Plausible with supporting logic, some uncertainty
    - **LOW:** Speculative but worth tracking; single line of evidence or contrarian signal
    ```

    **`templates/counter-scenario.md`** — Counter-scenario template (per D-07, D-11, D-12):
    ```markdown
    ---
    title:
    status: draft
    created: <% tp.date.now("YYYY-MM-DD") %>
    updated: <% tp.date.now("YYYY-MM-DD") %>
    tags: []
    alternative_thesis:
    divergence_points: []
    ---

    # {{title}}

    **Alternative Thesis:** `= this.alternative_thesis`
    **Status:** `= this.status`

    ## Thesis Statement

    <!-- One-paragraph description of this alternative path -->

    ## Key Divergence Points

    | Year/Event | Primary Timeline | Alternative Path |
    |------------|-----------------|------------------|
    | <!-- event --> | <!-- primary outcome --> | <!-- alternative outcome --> |

    ## Domain-by-Domain Implications

    ### Borders & Geopolitics

    <!-- How the alternative changes territorial configurations -->

    ### Climate

    <!-- Climate trajectory under this alternative -->

    ### Technology

    <!-- Technology development under this alternative -->

    ### Economy

    <!-- Economic structure under this alternative -->

    ### Demographics

    <!-- Population and migration patterns -->

    ### Culture

    <!-- Ideological and cultural landscape -->

    ## Likelihood Assessment

    <!-- Comparative assessment: why this is less likely than the primary, or key conditions that would make it more likely -->
    ```

    **`index.md`** — Master project index (root level). Per ARCHITECTURE.md section 5.4, this links to all milestones, transitions, and basemap:
    ```markdown
    ---
    title: Future Prediction — Project Index
    status: draft
    created: 2026-05-19
    updated: 2026-05-19
    ---

    # Future Prediction: Project Index

    A personal collection of structured forecasts mapping the evolution of global geopolitics from the present through the end of the 21st century. Central thesis: the collapse of the US empire and transition from capitalism to socialism, detailed enough to draw accurate maps at 2050, 2075, and 2100.

    ## Milestones

    | Milestone | Status | Map |
    |-----------|--------|-----|
    | [2050 Snapshot](2050-snapshot/index.md) | — | `2050-snapshot/kml/` |
    | [2075 Snapshot](2075-snapshot/index.md) | — | `2075-snapshot/kml/` |
    | [2100 Snapshot](2100-snapshot/index.md) | — | `2100-snapshot/kml/` |

    ## Transitions

    - [2026-2050: Collapse & Realignment](2026-2050-transition/) — Present day through the dissolution of the US and emergence of new order
    - [2051-2075: Transitional Chaos](2075-snapshot/) — The turbulent middle period
    - [2076-2100: Consolidation](2100-snapshot/) — The final arc to 2100

    ## Basemap

    - [Modern-Day Borders](BASEMAP_README.md) — Starting point for all projections

    ## Methodology

    - [Templates](templates/) — YAML frontmatter templates for domain docs, predictions, and counter-scenarios
    - [Prediction Register](meta/predictions/) — Falsifiable claims with confidence levels and target milestone dates
    - [Counter-Scenario](meta/counter-scenario.md) — Structured alternative to the primary collapse thesis
    - [Consistency Check](meta/consistency-check.md) — Cross-domain consistency mechanism

    ## Cross-Domain Consistency Map

    | Claim | Appears In | Also Affects | Status |
    |-------|-----------|--------------|--------|
    | _(Populated during milestone writing)_ | | | |

    ---

    *Last updated: 2026-05-19*
    ```
  </action>
  <verify>
    <automated>
      grep -c "title:" templates/base.md && grep -c "domain:" templates/domain-doc.md && grep -c "confidence:" templates/prediction.md && grep -c "alternative_thesis:" templates/counter-scenario.md && grep -c "## Milestones" index.md && echo "ALL_TEMPLATES_AND_INDEX_OK" || echo "MISSING_CONTENT"
    </automated>
  </verify>
  <acceptance_criteria>
    - templates/base.md contains "title:", "status:", "created:", "updated:", "tags:"
    - templates/domain-doc.md contains "domain:", "milestone:", "→ See KML:", "## Interactions With Other Domains"
    - templates/prediction.md contains "confidence:", "target_milestone:", "falsifiable_statement:", "domain:", "doc_ref:"
    - templates/counter-scenario.md contains "alternative_thesis:", "divergence_points:", "## Domain-by-Domain Implications"
    - index.md contains "2050 Snapshot", "2075 Snapshot", "2100 Snapshot", "Prediction Register", "Counter-Scenario"
  </acceptance_criteria>
  <done>
    All four YAML templates created with correct frontmatter schemas, root index.md links to all project components.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify vault initialization and template functionality</name>
  <what-built>
    - Obsidian vault configuration in .obsidian/
    - Full directory tree (templates/, meta/, milestones/, sources/, basemap/)
    - Four YAML frontmatter templates (base, domain-doc, prediction, counter-scenario)
    - Root index.md with master project navigation
    - BASEMAP_README.md documenting the modern-day KML basemap
    - .gitignore for generated/binary files
  </what-built>
  <how-to-verify>
    1. Open Obsidian → "Open folder as vault" → select the `future-prediction` repo directory
    2. Verify the file explorer shows the expected directory structure (12 folders)
    3. Check Settings → Community Plugins → Dataview is installed and enabled
    4. Create a test note:
       - Create a new note (Cmd+N)
       - In frontmatter, type `---` then check that autocomplete shows template fields
       - Or: Ctrl/Cmd+P → "Templates: Insert template" → choose "Domain Doc" template
       - Fill in sample values for title, domain, milestone
       - Verify the Dataview fields render correctly in Reading view (`= this.domain` shows the value)
    5. Open index.md — verify all links are present and the Cross-Domain Consistency Map section exists
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No applicable trust boundaries for Phase 1. All work is local file creation and Obsidian configuration. No network services, no authentication, no user input processing, no data at rest beyond local markdown files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .obsidian/ config files | Accept | Solo project with local-only git tracking; no shared state |
| T-01-02 | Information Disclosure | Prediction register content | Accept | Solo project, no planned publication; git repo is private |
| T-01-03 | Denial of Service | Obsidian vault | Accept | Local files only; git provides restore capability |
| T-01-04 | Elevation of Privilege | Template injection via YAML | Accept | Markdown-only, no code execution path; Obsidian sandboxes plugin execution |
</threat_model>

<verification>
Filesystem checks:

```bash
test -d .obsidian && echo "VAULT_INIT_OK" || echo "VAULT_MISSING"
test -d templates && echo "TEMPLATES_DIR_OK" || echo "TEMPLATES_DIR_MISSING"
test -d meta/predictions && echo "PREDICTIONS_DIR_OK" || echo "PREDICTIONS_DIR_MISSING"
test -d 2050-snapshot/domains && echo "2050_DOMAINS_DIR_OK" || echo "2050_DIR_MISSING"
grep -q "dataview" .obsidian/community-plugins.json && echo "DATAVIEW_ENABLED" || echo "DATAVIEW_MISSING"
```

Template schema verification:

```bash
for tmpl in templates/base.md templates/domain-doc.md templates/prediction.md templates/counter-scenario.md; do
  test -f "$tmpl" && echo "EXISTS: $tmpl" || echo "MISSING: $tmpl"
done
grep -c "title:" templates/base.md
grep -c "domain:" templates/domain-doc.md
grep -c "confidence:" templates/prediction.md
grep -c "alternative_thesis:" templates/counter-scenario.md
```
</verification>

<success_criteria>
- [ ] All directories in the flat per-milestone structure exist
- [ ] .obsidian/ vault config enables Dataview plugin
- [ ] All four YAML frontmatter templates exist with correct schemas
- [ ] BASEMAP_README.md documents modern-day basemap
- [ ] .gitignore excludes *.kmz and .DS_Store
- [ ] Human confirms the vault opens in Obsidian and templates are usable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-methodology/01-01-SUMMARY.md`
</output>
