---
phase: 20-africa-and-america-re-review
plan: 02
type: execute
wave: 2
depends_on:
  - "20-01-PLAN.md"
files_modified:
  - 2050-snapshot/kml/entity-config.json
  - 2050-snapshot/kml/user_colors.json
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/generate-kml.py
  - 2050-snapshot/kml/source/
autonomous: true
requirements:
  - AFAM-01
user_setup: []
must_haves:
  truths:
    - "entity-config.json reflects the canonical Nigeria state allocation from DISCOVERY.md — AES and Nigeria admin1_regions updated per Muslim-majority + contiguity criteria"
    - "entity-config.json has Cameroon split into 3 entities: AES North (admin1 regions added to AES), Ambazonia (new entity), Rump Cameroon (reduced CMR admin1)"
    - "CAC reassessment resolution from DISCOVERY.md is reflected: either CAR admin1 regions added to EAF, or CAR retains full admin1 with narrative-only change"
    - "Ambazonia entry exists in entity-config.json with correct NW+SW regions"
    - "User_colors.json has Ambazonia color entry"
    - "JSON validates: python3 -c 'import json; json.load(open(...))' exits 0"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Updated Africa entity definitions with correct Nigeria partition, Cameroon fragmentation, CAR resolution"
      contains: "Federation of Sahel States|Nigeria|Ambazonia"
    - path: "2050-snapshot/kml/user_colors.json"
      provides: "Color entry for Ambazonia"
      contains: "Ambazonia"
  key_links:
    - from: "20-DISCOVERY.md"
      to: "entity-config.json"
      via: "Nigeria state allocation table → admin1_regions update"
      pattern: "NGA"
    - from: "20-DISCOVERY.md"
      to: "entity-config.json"
      via: "Cameroon fragmentation → AES admin1 additions + Ambazonia entry + rump Cameroon"
      pattern: "CMR"
---

<objective>
Implement all KML and entity-config changes for Phase 20: Sahel-Nigeria border reallocation, Cameroon fragmentation (AES North + Ambazonia + Rump Cameroon), and CAR reassessment resolution.

Purpose: These are the only KML changes in Phase 20 per D-02. Getting entity-config.json correct is prerequisite for all documentation work — borders-geopolitics.md and domain doc entity profiles must reference the correct entity set.

Output: Updated entity-config.json, user_colors.json, regenerated borders.kml reflecting all Africa border changes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-africa-and-america-re-review/20-CONTEXT.md
@.planning/phases/20-africa-and-america-re-review/20-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update entity-config.json for Sahel-Nigeria border, Cameroon fragmentation, and CAR resolution</name>
  <files>2050-snapshot/kml/entity-config.json</files>
  <action>
**IMPORTANT: Read 20-DISCOVERY.md first. The canonical Nigeria allocation table and CAR reassessment determination from Plan 01 research MUST be implemented exactly as documented.**

**Step 1 — Read DISCOVERY.md for canonical determinations:**
Read `.planning/phases/20-africa-and-america-re-review/20-DISCOVERY.md`. Extract:
- Nigeria state allocation table (which states move between AES and Nigeria)
- Cameroon fragmentation entity structure
- CAR reassessment outcome (admin1 absorption or narrative-only)

**Step 2 — Update Nigeria states in Federation of Sahel States entry:**
In entity-config.json, find the `"Federation of Sahel States"` entry (line ~387 area). Replace the `admin1_regions` array:

1. Remove states that DISCOVERY.md allocates to Nigeria (FCT must move per D-04; borderline states per research)
2. Add northern Cameroon admin1 regions (Adamawa, North, Extreme North) per D-05
3. Preserve all non-NGA admin1 regions (MLI, BFA, NER, MRT, TGO — these are the core AES states)

Expected structure after edit:
```json
"Federation of Sahel States": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "country_codes": ["MLI", "BFA", "NER", "MRT", "TGO"],
  "admin1_regions": [
    // AES-allocated Nigerian states from DISCOVERY.md
    {"country_code": "NGA", "admin1_name": "Sokoto"},
    // ... (remaining AES states per DISCOVERY.md)
    // Northern Cameroon regions added to AES
    {"country_code": "CMR", "admin1_name": "Adamaoua"},
    {"country_code": "CMR", "admin1_name": "Nord"},
    {"country_code": "CMR", "admin1_name": "Extrême-Nord"}
  ],
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "federation-of-sahel-states",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#federation-of-sahel-states"
}
```

Note: GADM admin1 names for Cameroon may use French names (Adamaoua, Nord, Extrême-Nord) or English equivalents. Use exact names from the GADM dataset that the KML generator uses — check existing entity-config.json patterns for CMR admin1 naming.

**Step 3 — Update Nigeria entry — add FCT and states returning from AES:**
Find the `"Nigeria"` entry. Add FCT and any states that DISCOVERY.md allocates to Nigeria from the current AES set. Remove any states DISCOVERY.md allocates to AES that are currently in Nigeria.

```json
"Nigeria": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "admin1_regions": [
    // Current Nigeria states (southern states preserved)
    {"country_code": "NGA", "admin1_name": "Lagos"},
    // ... (all remaining Nigeria-allocated states)
    // FCT added per D-04
    {"country_code": "NGA", "admin1_name": "Federal Capital Territory"}
  ],
  // ... rest of entry preserved
}
```

**Step 4 — Create Ambazonia entity entry:**
After the Cameroon entry (line ~1578), insert the new Ambazonia entry:

```json
"Ambazonia": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "country_codes": ["CMR"],
  "admin1_regions": [
    {"country_code": "CMR", "admin1_name": "North-West"},
    {"country_code": "CMR", "admin1_name": "South-West"}
  ],
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "ambazonia",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md#ambazonia"
}
```

Use GADM admin1 names for Northwest and Southwest regions — may be "North-West" and "South-West" or "Nord-Ouest" and "Sud-Ouest". Check the GADM dataset.

The `keep_unified: true` signals to the KML generator that Ambazonia's admin1 regions should be rendered as a single unified polygon (not separate sub-polygons per region) per D-05.

**Step 5 — Update Cameroon entry (rump):**
Modify the existing Cameroon entry. Add `admin1_regions` listing only the Francophone southern regions. Add `keep_unified: true`. The source changes from `"country"` to `"group"` since it's now a subset of CMR:

```json
"Cameroon": {
  "type": "entity",
  "category": "global",
  "source": "group",
  "keep_unified": true,
  "country_codes": ["CMR"],
  "admin1_regions": [
    {"country_code": "CMR", "admin1_name": "Littoral"},
    {"country_code": "CMR", "admin1_name": "Centre"},
    {"country_code": "CMR", "admin1_name": "Sud"},
    {"country_code": "CMR", "admin1_name": "Est"},
    {"country_code": "CMR", "admin1_name": "Ouest"}
  ],
  "domain_doc": "2050-snapshot/domains/borders-geopolitics.md",
  "section_anchor": "",
  "see_path": "See: 2050-snapshot/domains/borders-geopolitics.md"
}
```

Note: Adamawa region is split between AES (the northern, Muslim-majority part) and Rump Cameroon (the southern, Francophone part). Since GADM admin1 is at the region level, Adamaoua goes to AES per D-05. The rump Cameroon entry does NOT include Adamaoua.

**Step 6 — CAR reassessment resolution:**
If DISCOVERY.md determines EAF absorption is **plausible**:
- Find EAF entry. Add admin1_regions with CAR southern prefectures (using country_code "CAF" or the appropriate GADM code). If EAF is `source: manual` (manual_path), adding admin1_regions may require converting it to `source: group` with the existing manual polygon as a manual addition.
- Find CAR entry. Remove the absorbed admin1 regions. Change CAR's source if needed to reflect reduced territory.

If DISCOVERY.md determines EAF absorption is **NOT plausible**:
- Leave entity-config.json Africa entries unchanged for CAR and EAF
- Note in SUMMARY: "CAR KML structure unchanged — narrative revisions will be in borders-geopolitics.md (Plan 03)"

**Step 7 — Add Ambazonia to folder hierarchy:**
Find the `"Middle Africa"` array in the `folder_hierarchy` section (around line 77). Add `"Ambazonia"` to the array (alphabetically, after "Angola").

**Step 8 — Validate JSON:**
Run: `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json')); print('PASS: JSON valid')"` — must exit 0.
  </action>
  <verify>
    <automated>python3 -c "import json; f=open('2050-snapshot/kml/entity-config.json'); data=json.load(f); assert 'Ambazonia' in data['entities'], 'Ambazonia entity missing'; assert 'Federation of Sahel States' in data['entities'], 'AES entity missing'; assert 'Nigeria' in data['entities'], 'Nigeria entity missing'; assert 'Cameroon' in data['entities'], 'Cameroon entity missing'; print('PASS: all entities present')"</automated>
    <automated>python3 -c "import json; f=open('2050-snapshot/kml/entity-config.json'); data=json.load(f); fct = [r for r in data['entities']['Nigeria'].get('admin1_regions',[]) if 'Federal Capital Territory' in r.get('admin1_name','')]; assert len(fct) > 0, 'FCT not in Nigeria entry'; print('PASS: FCT in Nigeria')"</automated>
    <automated>python3 -c "import json; f=open('2050-snapshot/kml/entity-config.json'); data=json.load(f); amb = data['entities']['Ambazonia']; assert amb.get('source') == 'group', 'Ambazonia should be group source'; print('PASS: Ambazonia entry valid')"</automated>
  </verify>
  <done>
    - entity-config.json has updated AES admin1_regions reflecting canonical Nigeria allocation and northern Cameroon addition
    - Nigeria entry has FCT added, state allocation matched to DISCOVERY.md
    - Ambazonia entity entry exists with NW+SW admin1 regions
    - Cameroon entry reduced to Francophone south (5 regions)
    - CAR resolution reflected (admin1 added to EAF or unchanged)
    - Valid JSON
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Ambazonia color entry in user_colors.json and regenerate borders.kml</name>
  <files>2050-snapshot/kml/user_colors.json, 2050-snapshot/kml/borders.kml, 2050-snapshot/kml/generate-kml.py</files>
  <action>
**Step 1 — Add Ambazonia color to user_colors.json:**
Read `user_colors.json` and find the existing "Cameroon" entry. Add an "Ambazonia" entry with a distinctive color. Use a yellow-gold or orange tone to distinguish from Cameroon's color:

```json
"Ambazonia": {
  "hex": "#e6c220",
  "kml_fill": "8020c2e6",
  "kml_line": "ff20c2e6",
  "width": 1.0
}
```

Adjust the hex color to a unique value that doesn't conflict with nearby entities. Validate JSON after edit.

**Step 2 — Check if KML generation is needed:**
Read `generate-kml.py` to understand the KML generation pipeline. Determine whether:
a) The entity-config changes require full KML regeneration (if the generator reads entity-config directly)
b) The borders.kml needs manual polygon extraction (if fragmented entities use manual paths)

Check whether the KML pipeline handles `source: "group"` with `admin1_regions` from GADM for the affected entities (AES with CMR regions, Nigeria with FCT, Ambazonia as new group entity, Cameroon as reduced group entity).

**Step 3 — Regenerate or manually create KML entities:**
If the generator supports the entity-config changes, run:
```bash
python3 2050-snapshot/kml/generate-kml.py
```

If manual KML paths are needed for fragmented entities (following the Saudi fragmentation pattern from Phase 13), create manual KML files:
- `source/manual/Ambazonia.kml` — polygon covering NW+SW regions of Cameroon (extracted from the CMR GADM dataset)

Check the existing `source/manual/` directory for examples of the approach (e.g., Saudi fragments).

**Step 4 — Add Ambazonia to borders.kml folder hierarchy:**
If the generator doesn't handle folder hierarchy automatically, add Ambazonia to the Middle Africa folder in borders.kml:
- Find the "Middle Africa" Folder in borders.kml
- Add a `<Folder>` element for Ambazonia (or a `<Placemark>` if using the entity's polygon)
- If using manual KML: `<NetworkLink>` or `<Folder>` with `<GroundOverlay>` as appropriate

Follow the existing pattern used in borders.kml for other group-source entities.

**Step 5 — Validate:**
- JSON valid: `python3 -c "import json; json.load(open('2050-snapshot/kml/user_colors.json')); print('PASS')"`
- KML well-formed: Check that borders.kml opens without XML errors
- All new entities referenced in both entity-config.json and borders.kml
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('2050-snapshot/kml/user_colors.json')); print('PASS: user_colors valid')"</automated>
    <automated>grep -c "Ambazonia" 2050-snapshot/kml/user_colors.json | grep -q "^1$" && echo "PASS: Ambazonia color exists" || echo "FAIL"</automated>
    <automated>grep -c "Ambazonia" 2050-snapshot/kml/borders.kml 2>/dev/null | grep -qE "^[1-9]" && echo "PASS: Ambazonia in borders.kml" || echo "WARN: Ambazonia may use NetworkLink or be absent from generated KML"</automated>
  </verify>
  <done>
    - user_colors.json has Ambazonia entry with valid color
    - borders.kml reflects all entity-config changes (via regeneration or manual addition)
    - All new KML entities render correctly in structure
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KML generator | entity-config.json consumed by Python KML generation script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | entity-config.json | mitigate | JSON validation after edit. All admin1 edits sourced from DISCOVERY.md canonical table. |
| T-20-04 | Tampering | user_colors.json | mitigate | JSON validation. Color choice is cosmetic (user adjusts in Google Earth). |
| T-20-05 | Tampering | borders.kml | mitigate | KML generation preserves existing content for unaffected regions. Manual edit only for Ambazonia addition. |
</threat_model>

<verification>
- entity-config.json, user_colors.json both valid JSON
- All 3 KML changes implemented: Sahel-Nigeria, Cameroon fragmentation, CAR resolution
- DISCOVERY.md canonical determinations faithfully reflected in entity-config
- SUMMARY documents the exact state allocation and CAR outcome
</verification>

<success_criteria>
entity-config.json and user_colors.json updated with all Africa KML border changes. Ambazonia created as new entity. Nigeria partition reflects canonical research. CAR reassessment implemented per DISCOVERY.md determination. borders.kml regenerated or manually updated.
</success_criteria>

<output>
After completion, create `.planning/phases/20-africa-and-america-re-review/20-02-SUMMARY.md`
</output>
