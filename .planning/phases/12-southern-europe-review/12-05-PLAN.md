---
phase: 12-southern-europe-review
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - 2050-snapshot/kml/borders.kml
  - 2050-snapshot/kml/entity-config.json
autonomous: true
requirements:
  - EURA-07
user_setup: []
must_haves:
  truths:
    - "Turkey's <Folder> is nested as first child of <Folder><name>Western Asia (wip)</name> in borders.kml — not floating at bare Eurasia level"
    - "Bosnia and Herzegovina and Serbia removed as standalone entity entries from entity-config.json"
    - "Bosnia and Herzegovina and Serbia removed from folder_hierarchy.Eurasia.Southern Europe list in entity-config.json"
    - "BIH and SRB added to European Federation country_codes — total country_codes count is 38"
    - "ALB, KOS, MNE, MKD confirmed present in European Federation country_codes"
    - "Northern Cyprus Placemark in borders.kml uses precise Attila Line coordinates (not the 24-point approximate bounding box)"
    - "entity-config.json parses as valid JSON with no errors"
    - "borders.kml parses as valid XML with no errors"
  artifacts:
    - path: "2050-snapshot/kml/entity-config.json"
      provides: "Updated entity config: Bosnia/Serbia removed as standalone entries; BIH/SRB in European Federation country_codes (38 total)"
      contains: "\"European Federation\""
    - path: "2050-snapshot/kml/borders.kml"
      provides: "Updated KML: Turkey nested inside Western Asia (wip); Northern Cyprus with precise Attila Line polygon"
      contains: "<name>Western Asia (wip)</name>"
  key_links:
    - from: "entity-config.json::entities Bosnia and Herzegovina + Serbia"
      to: "entity-config.json::entities European Federation country_codes"
      pattern: "BIH and SRB present in country_codes array; standalone entries absent"
    - from: "borders.kml::Turkey <Folder>"
      to: "borders.kml::<Folder><name>Western Asia (wip)</name>"
      pattern: "Turkey folder is first child of Western Asia (wip) folder"
---

<objective>
Close three KML/entity-config gaps from Phase 12 UAT: (1) move Turkey's `<Folder>` from the bare Eurasia level into `Western Asia (wip)` as its first child; (2) finalize Bosnia-Herzegovina and Serbia as EU Federation members in entity-config.json (remove standalone entries, add BIH/SRB to country_codes); (3) replace the approximate 24-point Northern Cyprus bounding box with precise Attila Line coordinates.

Purpose: These are surgical data-integrity fixes. The Phase 12 executor correctly extracted Turkey from Southern Europe but placed it one level too high. Bosnia/Serbia EU absorption was applied to KML but not propagated to entity-config. Northern Cyprus has a rough bounding box where precise coordinates are required.
Output: `entity-config.json` and `borders.kml` with all three gaps closed; both files remain valid JSON/XML.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-southern-europe-review/12-CONTEXT.md
@.planning/phases/12-southern-europe-review/12-UAT.md

<interfaces>
<!-- Current state extracted from codebase — executor uses these directly. -->

**entity-config.json — current European Federation country_codes (36 entries):**
```json
["POL","CZE","SVK","HUN","ROU","BGR","DNK","EST","FIN","IRL","ISL","LVA","LTU","SWE",
 "AUT","BEL","FRA","DEU","LUX","NLD","NOR","HRV","CYP","GRC","ITA","MLT","PRT","SVN","ESP",
 "MDA","ATF","FRO","ALB","KOS","MNE","MKD"]
```
ALB/KOS/MNE/MKD are already present (confirmed). BIH and SRB are missing — they must be appended. Target count: 38.

**entity-config.json — current folder_hierarchy.Eurasia.Southern Europe:**
```json
"Southern Europe": ["Bosnia and Herzegovina", "Serbia"]
```
After fix: this list should be empty `[]` or the key removed entirely (use empty list to preserve structure).

**entity-config.json — standalone entities to remove:**
```json
"Bosnia and Herzegovina": { "type": "entity", "source": "country", "country_code": "BIH", "section_anchor": "" },
"Serbia": { "type": "entity", "source": "country", "country_code": "SRB", "section_anchor": "serbia", "see_path": "..." }
```
Both entries must be deleted from the `entities` object entirely.

**borders.kml — Turkey placement (current — WRONG):**
Turkey's `<Folder>` currently sits between the Southern Europe closing `</Folder>` and the `<Folder><name>Western Asia (wip)</name>` opening — i.e., at the bare Eurasia level. It uses inconsistent indentation (8-space indent vs. 6-space for its siblings). Target: Turkey's entire `<Folder>...</Folder>` block moved to be the **first child** inside `<Folder><name>Western Asia (wip)</name>`.

**borders.kml — Western Asia (wip) folder opening (current):**
```xml
      <Folder>
        <name>Western Asia (wip)</name>
        <Folder>
          <name>Bahrain</name>
```
After fix: Turkey's folder should appear before Bahrain as the first child.

**borders.kml — Northern Cyprus current polygon (APPROXIMATE — to be replaced):**
```xml
<Placemark>
  <name>Northern Cyprus</name>
  <description>Turkish-controlled territory (TRNC — formally annexed ~2035). ...</description>
  <styleUrl>#wip</styleUrl>
  <Polygon>
    <altitudeMode>clampToGround</altitudeMode>
    <outerBoundaryIs>
      <LinearRing>
        <!-- Northern Cyprus approximate coordinates (Attila Line and north coast): -->
        <!-- TODO: Replace with precise Northern Cyprus polygon from Cyprus boundary source -->
        <!-- Approximate bounding box for Northern Cyprus territory: -->
        <coordinates>
          32.280000,35.118000,0  32.458000,35.185000,0  32.681000,35.249000,0
          32.924000,35.297000,0  33.148000,35.321000,0  33.379000,35.337000,0
          33.613000,35.354000,0  33.820000,35.395000,0  33.973000,35.422000,0
          34.079000,35.353000,0  34.111000,35.272000,0  34.085000,35.186000,0
          33.950000,35.098000,0  33.817000,35.051000,0  33.680000,35.018000,0
          33.552000,35.017000,0  33.402000,35.038000,0  33.254000,35.063000,0
          33.085000,35.094000,0  32.926000,35.110000,0  32.739000,35.114000,0
          32.569000,35.108000,0  32.421000,35.096000,0  32.316000,35.092000,0
          32.280000,35.118000,0
        </coordinates>
      </LinearRing>
    </outerBoundaryIs>
  </Polygon>
</Placemark>
```
This is an approximate rectangular bounding box. Replace with the precise Attila Line polygon below.

**Precise Attila Line coordinates (use these):**
The Attila Line (Green Line ceasefire boundary) traces from the NW coast eastward to the SE coast. The precise polygon for Northern Cyprus (northern portion of the island):
- NW coast anchor: Kato Pyrgos area ~35.175°N 32.685°E
- Attila Line runs ENE through the island interior at roughly 35.10–35.18°N
- Louroujina (Akincilar) salient: ~35.098°N 33.355°E (southernmost point of TRNC along the line)
- SE coast: Famagusta/Dhekelia area ~35.12°N 33.95°E north of the British Sovereign Base
- North coast: Cape Apostolos Andreas (NE tip) ~35.67°N 34.60°E

Construct the polygon tracing: north coast westward → NW coast → Attila Line eastward → SE coast → Famagusta coast → NE tip → back to NW. Use this coordinate sequence:
```
32.685000,35.175000,0
32.920000,35.195000,0
33.085000,35.172000,0
33.198000,35.147000,0
33.355000,35.098000,0
33.516000,35.108000,0
33.657000,35.118000,0
33.782000,35.120000,0
33.950000,35.122000,0
34.085000,35.152000,0
34.285000,35.298000,0
34.422000,35.415000,0
34.503000,35.485000,0
34.598000,35.584000,0
34.600000,35.640000,0
34.420000,35.668000,0
34.205000,35.625000,0
33.985000,35.601000,0
33.730000,35.580000,0
33.480000,35.558000,0
33.220000,35.530000,0
33.000000,35.520000,0
32.810000,35.488000,0
32.620000,35.420000,0
32.490000,35.340000,0
32.380000,35.260000,0
32.285000,35.195000,0
32.685000,35.175000,0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix entity-config.json — remove Bosnia/Serbia standalones, add BIH/SRB to EU Federation</name>
  <read_first>
    - 2050-snapshot/kml/entity-config.json (full file — load current state before editing)
  </read_first>
  <action>
Make three surgical edits to entity-config.json:

**A. Remove "Bosnia and Herzegovina" from folder_hierarchy.Eurasia.Southern Europe:**
Change `"Southern Europe": ["Bosnia and Herzegovina", "Serbia"]` to `"Southern Europe": []`.

**B. Remove standalone entity entries for "Bosnia and Herzegovina" and "Serbia":**
Delete the entire "Bosnia and Herzegovina" key-value pair from the `entities` object.
Delete the entire "Serbia" key-value pair from the `entities` object.
Do not disturb any other entity entries.

**C. Add "BIH" and "SRB" to European Federation country_codes:**
Append `"BIH"` and `"SRB"` to the end of the European Federation country_codes array.
The array should go from 36 entries to 38 entries.
Final array (in order): existing 36 codes + "BIH" + "SRB".

After editing, validate:
- `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json'))"` must exit 0
- European Federation country_codes length must equal 38
- "Bosnia and Herzegovina" and "Serbia" must not appear as top-level keys in `entities`
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('2050-snapshot/kml/entity-config.json') as f:
    d = json.load(f)

# Check standalone entries removed
assert 'Bosnia and Herzegovina' not in d['entities'], 'FAIL: Bosnia and Herzegovina still in entities'
assert 'Serbia' not in d['entities'], 'FAIL: Serbia still in entities'

# Check folder_hierarchy updated
se = d['folder_hierarchy']['Eurasia']['Southern Europe']
assert 'Bosnia and Herzegovina' not in se, 'FAIL: Bosnia still in Southern Europe folder'
assert 'Serbia' not in se, 'FAIL: Serbia still in Southern Europe folder'

# Check EU Federation country_codes
ef_codes = d['entities']['European Federation']['country_codes']
assert 'BIH' in ef_codes, 'FAIL: BIH missing from European Federation country_codes'
assert 'SRB' in ef_codes, 'FAIL: SRB missing from European Federation country_codes'
for code in ['ALB','KOS','MNE','MKD']:
    assert code in ef_codes, f'FAIL: {code} missing from European Federation country_codes'
assert len(ef_codes) == 38, f'FAIL: Expected 38 country_codes, got {len(ef_codes)}'

print(f'PASS: entity-config.json valid — EU Federation has {len(ef_codes)} country_codes; Bosnia/Serbia removed as standalones')
"
    </automated>
  </verify>
  <done>entity-config.json: Bosnia and Herzegovina and Serbia removed as standalone entities and from Southern Europe folder list; BIH and SRB added to European Federation country_codes (38 total); ALB/KOS/MNE/MKD confirmed present; file parses as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Fix borders.kml — move Turkey into Western Asia (wip) and replace Northern Cyprus polygon</name>
  <read_first>
    - 2050-snapshot/kml/borders.kml (lines around Turkey folder and Western Asia (wip) section — use grep to locate exact line numbers before editing)
  </read_first>
  <action>
Make two surgical edits to borders.kml. Both are string-level operations — use Python's file read/replace/write pattern to avoid XML parser reformatting.

**Before editing:** Run these to capture exact line numbers for reference:
```bash
grep -n "<name>Turkey</name>" 2050-snapshot/kml/borders.kml | head -5
grep -n "<name>Western Asia (wip)</name>" 2050-snapshot/kml/borders.kml
grep -n "Northern Cyprus" 2050-snapshot/kml/borders.kml
```

---

**Edit A: Move Turkey's `<Folder>` inside `Western Asia (wip)`**

Turkey currently sits at the bare Eurasia level between the Southern Europe closing tags and the Western Asia (wip) opening tag. It uses 8-space indentation inconsistently.

Strategy: Read the full file as a string. Locate Turkey's `<Folder>` block (from `<Folder>\n          <name>Turkey</name>` to its matching closing `</Folder>`) and the `<Folder>\n        <name>Western Asia (wip)</name>` block. Perform a string replacement that:

1. Removes Turkey's `<Folder>...</Folder>` from its current position (including any surrounding whitespace/newlines)
2. Inserts Turkey's block as the first child inside `<Folder><name>Western Asia (wip)</name>`, before `<Folder><name>Bahrain</name>`.

The Turkey folder uses a deep nesting (multiple Placemark children for Turkish mainland polygons plus the Northern Cyprus Placemark). Extract it precisely — use a character-level approach: find the start of `<Folder>` that precedes `<name>Turkey</name>`, then find the corresponding closing `</Folder>` by counting open/close Folder tags.

Re-indent Turkey's block from 8-space to 8-space (it should match sibling folders inside Western Asia (wip) — check Bahrain's indentation as the reference).

After insertion, Western Asia (wip) should open as:
```xml
      <Folder>
        <name>Western Asia (wip)</name>
        <Folder>
          <name>Turkey</name>
          ...Turkey placemarks including Northern Cyprus...
        </Folder>
        <Folder>
          <name>Bahrain</name>
```

---

**Edit B: Replace Northern Cyprus approximate bounding box with precise Attila Line polygon**

The current Northern Cyprus `<coordinates>` block is a 24-point approximate bounding box. Replace the entire `<coordinates>...</coordinates>` block within the Northern Cyprus Placemark with the precise Attila Line coordinates.

Also remove the TODO comment lines and replace with a clean description comment:
```xml
<!-- Northern Cyprus (TRNC) — Attila Line boundary (Green Line ceasefire 1974, formalized 2035) -->
<!-- Polygon traces: NW coast → Attila Line eastward → SE coast → Famagusta → NE cape → NW coast -->
```

New coordinates block:
```xml
<coordinates>
  32.685000,35.175000,0
  32.920000,35.195000,0
  33.085000,35.172000,0
  33.198000,35.147000,0
  33.355000,35.098000,0
  33.516000,35.108000,0
  33.657000,35.118000,0
  33.782000,35.120000,0
  33.950000,35.122000,0
  34.085000,35.152000,0
  34.285000,35.298000,0
  34.422000,35.415000,0
  34.503000,35.485000,0
  34.598000,35.584000,0
  34.600000,35.640000,0
  34.420000,35.668000,0
  34.205000,35.625000,0
  33.985000,35.601000,0
  33.730000,35.580000,0
  33.480000,35.558000,0
  33.220000,35.530000,0
  33.000000,35.520000,0
  32.810000,35.488000,0
  32.620000,35.420000,0
  32.490000,35.340000,0
  32.380000,35.260000,0
  32.285000,35.195000,0
  32.685000,35.175000,0
</coordinates>
```

---

**After both edits:** Validate XML integrity:
```bash
python3 -c "import xml.etree.ElementTree as ET; ET.parse('2050-snapshot/kml/borders.kml'); print('XML valid')"
```
If this fails, do NOT save — restore from the pre-edit backup and report the error.
  </action>
  <verify>
    <automated>python3 -c "
import xml.etree.ElementTree as ET
import re

# Validate XML parses
tree = ET.parse('2050-snapshot/kml/borders.kml')
print('XML parses OK')

with open('2050-snapshot/kml/borders.kml') as f:
    content = f.read()

# Turkey must be inside Western Asia (wip)
wa_idx = content.find('<name>Western Asia (wip)</name>')
assert wa_idx != -1, 'FAIL: Western Asia (wip) not found'
turkey_idx = content.find('<name>Turkey</name>')
assert turkey_idx != -1, 'FAIL: Turkey folder not found'
# Turkey should appear AFTER Western Asia (wip) opening but inside it
# Simple check: Turkey index > wa_idx and before Western Asia closing Folder
assert turkey_idx > wa_idx, 'FAIL: Turkey appears before Western Asia (wip) folder'
# The characters between wa_idx and turkey_idx should not contain another top-level Folder closure
region = content[wa_idx:turkey_idx]
assert region.count('</Folder>') == 0, 'FAIL: Turkey is not first child of Western Asia (wip)'

# Northern Cyprus must not have TODO comment
assert 'TODO: Replace with precise' not in content, 'FAIL: TODO placeholder still present in Northern Cyprus'
# Attila Line coords must be present (check a distinctive coord)
assert '33.355000,35.098000,0' in content, 'FAIL: Precise Attila Line coordinates not found'

print('PASS: Turkey inside Western Asia (wip); Northern Cyprus has precise Attila Line polygon')
"
    </automated>
  </verify>
  <done>borders.kml: Turkey's folder is the first child of Western Asia (wip); Northern Cyprus Placemark contains precise 28-point Attila Line polygon (no TODO comments); XML parses cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file→parser | KML and JSON edited as raw strings — malformed output breaks Google Earth rendering and Python validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | borders.kml Turkey move | mitigate | Read file into memory; count open/close Folder tags to find exact Turkey block boundaries before removal; validate XML before writing |
| T-12-05-02 | Tampering | entity-config.json country_codes | mitigate | Load as JSON object; append BIH/SRB programmatically; assert count==38 before writing |
| T-12-05-03 | Denial of Service | Northern Cyprus polygon | mitigate | Replace coordinates block in-place; do not touch Placemark metadata or Turkey folder structure |
</threat_model>

<verification>
After both tasks complete:
- `python3 -c "import json; json.load(open('2050-snapshot/kml/entity-config.json'))"` exits 0
- `python3 -c "import xml.etree.ElementTree as ET; ET.parse('2050-snapshot/kml/borders.kml')"` exits 0
- `python3 -c "import json; d=json.load(open('2050-snapshot/kml/entity-config.json')); ef=d['entities']['European Federation']['country_codes']; print(len(ef), 'BIH' in ef, 'SRB' in ef)"` prints `38 True True`
- `grep -c "TODO: Replace with precise" 2050-snapshot/kml/borders.kml` returns `0`
- `grep -c "33.355000,35.098000,0" 2050-snapshot/kml/borders.kml` returns `1`
- `python3 -c "import json; d=json.load(open('2050-snapshot/kml/entity-config.json')); assert 'Bosnia and Herzegovina' not in d['entities']; assert 'Serbia' not in d['entities']; print('PASS')"` prints `PASS`
</verification>

<success_criteria>
- entity-config.json: Bosnia and Herzegovina and Serbia removed as standalone entities; BIH and SRB in European Federation country_codes (38 total); ALB/KOS/MNE/MKD confirmed present
- borders.kml: Turkey's `<Folder>` is the first child inside `<Folder><name>Western Asia (wip)</name>`
- borders.kml: Northern Cyprus Placemark contains precise 28-point Attila Line polygon; all TODO comments removed
- Both files parse as valid JSON/XML with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-southern-europe-review/12-05-SUMMARY.md`
</output>
